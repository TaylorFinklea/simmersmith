# SP-A Phase 4-remainder + Phase 5 — design blueprints + multi-lens reviews (Sonnet/Haiku workflow)

## Phase 4-remainder — Week/WeekMeal/EventGroceryItem/Event codecs + Event/EventGroceryItem merger cases + post-batch repair-pass wiring + schema

### Blueprint (Sonnet)

**goal:**
Wire Week/WeekMeal/EventGroceryItem/Event codecs + EventSyncMerger + post-batch repair pass into HouseholdSyncEngine; widen EventGroceryItem value type; add weekEnd to Week; author missing CKDSL blocks; build headless-testable EventMergeEngine for the event↔week orchestration.

**approach:**
Seven sequenced work items, each independently committable. Read the referenced files before touching any of them — do not trust field names or signatures from this prompt alone.

---

## Work item 1: Value-type expansion (Models.swift — GroceryMerge target, CloudKit-free)

**What to read first:** `SimmerSmithCloudKit/Sources/GroceryMerge/Models.swift` (full file, already read), `app/models/event.py:213-254` (EventGroceryItem ORM), `app/models/week.py:25-26` (Week.week_end).

**Changes — all additive with defaults so existing call sites and tests compile unchanged:**

`EventGroceryItem` struct: add 11 fields with defaults. Names map to prod ORM columns (event.py:213-254):
- `totalQuantity: Double?` (= prod `total_quantity` — the event row's own aggregate contribution; distinct from GroceryItem.eventQuantity which accumulates across events)
- `baseIngredientID: String?`
- `ingredientVariationID: String?`
- `ingredientName: String` default `""`
- `normalizedName: String` default `""`
- `unit: String` default `""`
- `quantityText: String` default `""`
- `category: String` default `""`
- `sourceMeals: String` default `""`
- `notes: String` default `""`
- `reviewFlag: String` default `""`
- `resolutionStatus: String` default `"unresolved"`

Mirror the GroceryItem init pattern: all parameters keyword-with-defaults so the two existing EventGroceryItem constructors in `ResolverTests.swift` (lines 53-60) compile unchanged.

`Week` struct: add `weekEnd: String` default `""`. The Week init already uses keyword args; add `weekEnd: String = ""` before `modifiedAt`. The existing `ResolverTests.swift` Week constructor at line 77 takes only `recordName/weekStart/modifiedAt` — adding a defaulted param is additive-safe.

`Event` struct: change `eventDate: String` to `eventDate: String?` (nil = no date). Document in a comment that nil maps to Python's `event.event_date is None` and that any non-nil value MUST be ISO-8601 YYYY-MM-DD for lexical ordering to equal date ordering (load-bearing for _resolve_target_week). Update the init default from `""` to `nil`. The existing test at ResolverTests.swift:67 constructs `Event(recordName: "EV", eventDate: "2026-07-04", manuallyMerged: false, modifiedAt: 9)` — changing the field to Optional means the literal `"2026-07-04"` still works (it's `String?` accepted); the `modifiedDate.eventDate = false` line is not present, the compile impact is nil. Check whether `FieldMergeResolver.merge(Event)` reads `eventDate` — it does not (line 76-83); only `manuallyMerged` is touched there, so the Optional change has no impact on the resolver.

`GroceryItem`: add `storeLabel: String` default `""` (pass-through, not merge-relevant; prevents codec round-trip data loss for week.py:234). Mirror the pattern of `notes`/`reviewFlag`.

**Verify:** `swift test --package-path /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit` — all 52 existing tests green with the expanded types.

---

## Work item 2: EventGroceryItemCodec + EventCodec (HouseholdSync target, behind #if canImport(CloudKit))

**What to read first:** `SimmerSmithCloudKit/Sources/HouseholdSync/GroceryCodec.swift` (the established codec pattern to mirror exactly — encode/into:/decode/makeRecord/setOpt/recordType). `app/models/event.py:213-254` for EventGroceryItem field names. The existing Event CKDSL block at `phase0-schema.ckdb:187-201` for field names already in the schema.

**Create:** `SimmerSmithCloudKit/Sources/HouseholdSync/EventGroceryCodecs.swift`

Contains two enums, each following GroceryCodec exactly:

`EventGroceryItemCodec`:
- `static let recordType = "EventGroceryItem"`
- `makeRecord(_ item: EventGroceryItem, zoneID: CKRecordZone.ID) -> CKRecord`
- `encode(_ item: EventGroceryItem, into record: CKRecord)` — writes all 16 fields. Critical mapping decisions (irreversible CloudKit field names):
  - `ingredientName`, `normalizedName`, `unit`, `quantityText`, `category`, `sourceMeals`, `notes`, `reviewFlag`, `resolutionStatus` — plain String, never nil
  - `totalQuantity` — DOUBLE, optional via setOpt (prod name `total_quantity`; CKRecord field name `totalQuantity`)
  - `baseIngredientID`, `ingredientVariationID` — plain String keys (crossDBString, not CKReference)
  - `mergedIntoGroceryItemID`, `mergedIntoWeekID` — plain String keys (SET-NULL soft refs, §C; encoding as CKReference would be wrong)
  - `modifiedAtClock` — INT64 SyncClock (matches the pattern in GroceryCodec; NOT a TIMESTAMP)
  - The CASCADE parent `event REFERENCE` is set by the creation path that knows the event recordID, not by the codec. Mirror how RecipeIngredient's parent ref is set in the HouseholdRecordCodec — find that pattern and mirror it. The codec encodes merge-subset fields; the cascade parent ref is set separately at record-creation time.
- `decode(_ record: CKRecord) -> EventGroceryItem` — reads all fields with safe fallbacks (`as? String ?? ""`, `as? Double`, `as? Int ?? 0`)

`EventCodec`:
- `static let recordType = "Event"`
- The Event CKDSL block already exists (phase0-schema.ckdb:187-201) but is missing `modifiedAtClock INT64`. The codec must write `modifiedAtClock` as INT64 (mirror GroceryCodec pattern) even though the CKDSL block doesn't have it yet — Work item 3 adds it additively.
- `encode(_ item: Event, into record: CKRecord)`: `name` String, `eventDate` String? (nil → `record["eventDate"] = nil`; the CKDSL eventDate is TIMESTAMP SORTABLE but the Swift type is String? — encode as TIMESTAMP: map non-nil ISO-8601 date string to a `Date` via `ISO8601DateFormatter` with format `.withFullDate` before writing), `linkedWeekID` String? via setOpt, `autoMergeGrocery` and `manuallyMerged` as INT64 0/1, `modifiedAtClock` as INT64.
- `decode(_ record: CKRecord) -> Event`: reverse of above. For `eventDate`: read as `Date?`, format back to YYYY-MM-DD via `ISO8601DateFormatter` with `.withFullDate`, nil if absent. This enforces the ISO-8601 invariant at the codec boundary in both directions.

**Critical codec invariant to document in comments:** `EventCodec.encode/decode` is the ONLY place that maps between Swift `String?` eventDate and the CKDSL `TIMESTAMP` field. The formatter must use `.withFullDate` (YYYY-MM-DD only, not full ISO-8601 with time). If any other code writes eventDate to a CKRecord directly, the lexical ordering guarantee breaks silently.

**Verify:** `swift build --package-path /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit` — new file compiles under Swift 5 mode. Add a codec round-trip test in `HouseholdSyncTests/GroceryMergeSeamTests.swift` for both new codecs, mirroring `groceryCodecRoundTrips()` (line 13-26). Check: `EventGroceryItemCodec.decode(EventGroceryItemCodec.makeRecord(item, zoneID:)) == item` with all 16 fields set; same for EventCodec with a non-nil and nil eventDate.

---

## Work item 3: EventSyncMerger + MultiMerger (RecordMerger.swift extension)

**What to read first:** `SimmerSmithCloudKit/Sources/HouseholdSync/RecordMerger.swift` (the RecordMerger protocol and GrocerySyncMerger pattern, already read). `SimmerSmithCloudKit/Sources/HouseholdSync/HouseholdSyncEngine.swift` lines 28 and 150-163 (merger installation and dispatch).

**Changes to RecordMerger.swift** (or a sibling file `EventSyncMergers.swift` in HouseholdSync):

`EventGrocerySyncMerger: RecordMerger`:
- `handles(_ recordType: String) -> Bool`: `recordType == EventGroceryItemCodec.recordType`
- `resolve(local:remote:)`: decode both via `EventGroceryItemCodec.decode`, call `FieldMergeResolver.merge(localValue, remoteValue)`, encode merged onto a copy of remote (mirror GrocerySyncMerger exactly), return `MergeResult(record: mergedRecord, needsResave: merged != remoteValue)`

`EventSyncMerger: RecordMerger`:
- Same pattern for `EventCodec.recordType` / `EventCodec.decode` / `FieldMergeResolver.merge(Event)`

`CompositeMerger: RecordMerger`:
- Holds `[RecordMerger]`; dispatches to the first that `handles`. `handles` returns true if any element does.
- This replaces the `engine.merger` single-slot with a multi-type dispatcher.

**Changes to HouseholdSyncEngine.swift**: Change `public var merger: RecordMerger?` to accept a `CompositeMerger` or keep the existing single-slot and have the caller install a `CompositeMerger` that wraps all three. The simplest approach: keep the protocol slot unchanged; the caller sets `engine.merger = CompositeMerger([GrocerySyncMerger(), EventGrocerySyncMerger(), EventSyncMerger()])`. No engine structural change needed.

**Verify:** `swift build`. Add a test in `GroceryMergeSeamTests.swift`: construct a `CompositeMerger`, verify `handles("GroceryItem")` / `handles("EventGroceryItem")` / `handles("Event")` all return true, `handles("Recipe")` returns false. Add a resolver round-trip for EventGroceryItem via the merger (mirror `mergerPreservesLocalCheckOverLaterRemoteAuto` for the manuallyMerged pin on Event).

---

## Work item 4: CKDSL schema additions (phase0-schema.ckdb — irreversible, human-deployed)

**What to read first:** `phase0-schema.ckdb` lines 1-338 (full file, especially the GroceryItem block at 314-338 as the Phase-4 pattern to follow). `cloudkit-sp-a-phase0-schema.md` §A (recordName policy), §B (queryable indexes), §C (reference graph). The existing Event block at 187-201 (needs two additive fields).

**Append to phase0-schema.ckdb** after the GroceryItem block (line 338):

```
// ---- Phase 4-remainder: Week (cascade root for GroceryItem/WeekMeal) ----

RECORD TYPE Week (
    weekStart STRING QUERYABLE SORTABLE,
    weekEnd STRING,
    status STRING,
    notes STRING,
    readyForAIAt TIMESTAMP,
    approvedAt TIMESTAMP,
    pricedAt TIMESTAMP,
    createdAt TIMESTAMP,
    updatedAt TIMESTAMP,
    GRANT WRITE TO "_creator",
    GRANT READ, CREATE TO "_icloud"
);

// ---- Phase 4-remainder: WeekMeal (deferred from 2b; slot-repair needs
//      synced slot data for the post-batch repair pass) ----

RECORD TYPE WeekMeal (
    dayName STRING,
    slot STRING,
    sortOrder INT64,
    aiGenerated INT64,
    approved INT64,
    notes STRING,
    createdAt TIMESTAMP,
    updatedAt TIMESTAMP,
    week REFERENCE,
    GRANT WRITE TO "_creator",
    GRANT READ, CREATE TO "_icloud"
);

// ---- Phase 5: EventGroceryItem (CASCADE child of Event; merged_into_* are
//      plain STRING keys per §C SET-NULL — NOT CKReference) ----

RECORD TYPE EventGroceryItem (
    ingredientName STRING,
    normalizedName STRING QUERYABLE,
    totalQuantity DOUBLE,
    unit STRING,
    quantityText STRING,
    category STRING,
    sourceMeals STRING,
    notes STRING,
    reviewFlag STRING,
    resolutionStatus STRING,
    mergedIntoWeekID STRING,
    mergedIntoGroceryItemID STRING,
    modifiedAtClock INT64,
    createdAt TIMESTAMP,
    updatedAt TIMESTAMP,
    event REFERENCE,
    baseIngredientID STRING,
    ingredientVariationID STRING,
    GRANT WRITE TO "_creator",
    GRANT READ, CREATE TO "_icloud"
);
```

**Additive patch to the existing Event block**: The current Event block (lines 187-201) is missing `modifiedAtClock INT64`. Add it as an additive field to the existing block before the GRANT lines. Because the schema is cumulative and additive-only, adding a field to an existing type is safe — re-importing the whole file adds the new field without dropping existing data.

**Also patch GroceryItem block** (lines 314-338): add `weekRef REFERENCE` (the CASCADE parent per §C: Week→Grocery is CASCADE; this is the `.deleteSelf` parent CKReference) and `createdAtTimestamp TIMESTAMP SORTABLE` (§B: GroceryItem createdAt SORTABLE; the existing `createdAtClock INT64` holds the logical SyncClock for merge ordering; the TIMESTAMP copy enables CKQuery sort — keep both). Also add `storeLabel STRING` (prod week.py:234, additive, no merge logic).

**IRREVERSIBLE decisions to lock before user deploys:**
- `normalizedName STRING QUERYABLE` on EventGroceryItem — confirm this index is worth the storage before deploying to Production (it enables future CKQuery match-key lookups but isn't required by the current post-batch scan approach).
- `weekRef REFERENCE` on GroceryItem — this is a CASCADE parent (.deleteSelf in the codec); once deployed, deleting a Week record cascades to its GroceryItems on the deleting device. This is correct per §C but is a one-way schema decision.
- WeekMeal `week REFERENCE` — same CASCADE semantics.
- EventGroceryItem `event REFERENCE` — CASCADE parent; Event delete sweeps EventGroceryItems.

**User verify command (DO NOT run from this thread — needs management token):**
```
xcrun cktool validate-schema --team-id <TEAM_ID> \
  --container-id iCloud.app.simmersmith.cloud \
  --environment development \
  --file /Users/tfinklea/git/simmersmith/.docs/ai/phases/phase0-schema.ckdb
```
Then import to DEV (not Production) and confirm via `cktool export-schema` that all new types appear.

---

## Work item 5: Post-batch repair pass wired into HouseholdSyncEngine

**What to read first:** `HouseholdSyncEngine.swift` lines 143-204 (the full `handleEvent` implementation). `HouseholdLocalStore.swift` (the full file — especially `allRecords()` at line 22 and `recordIDsCascadingFrom` at line 52 as the scan pattern to mirror). `ConflictRepair.swift` (the full file — dedupeGrocery, repairDuplicateSlots, reconcileSortOrder, collapseDuplicateWeeks).

**Changes to HouseholdSyncEngine.swift** — inside the `.fetchedRecordZoneChanges` handler (lines 148-178), after the per-modification loop and before the function returns:

The repair pass needs a week-scoped sibling set. Use Option A (scan): after the loop, collect a `Set<String>` of `affectedWeekNames` by scanning the modifications for records whose `recordType == "GroceryItem"` (read the `weekRef` CKReference's recordID.recordName) or `"EventGroceryItem"` (read `mergedIntoWeekID` as String from the record). Then for each affected weekName:

1. `let allRecs = store.allRecords()` — single scan under lock.
2. Decode all `"GroceryItem"` records where the `weekRef` CKReference points to weekName → `[GroceryItem]` via `GroceryCodec.decode`.
3. Decode all `"EventGroceryItem"` records where `record["mergedIntoWeekID"] as? String == weekName` → `[EventGroceryItem]` via `EventGroceryItemCodec.decode`.
4. Call `ConflictRepair.dedupeGrocery(items: groceries, eventLinks: eventLinks)`.
5. From `GroceryDedupeResult`:
   - For each keeper: `store.setRecord(GroceryCodec.makeRecord(keeper, zoneID: zoneID))` then `syncEngine.state.add(pendingRecordZoneChanges: [.saveRecord(...)])` — but only if keeper.recordName is in the tombstoned set (i.e. it was modified by the dedupe). More precisely: only enqueue a save if `result.keepers` contains a record that differs from what was in the store before (compare decoded values). Simplest correct approach: always enqueue saves for keepers that have more than one item in their deduplication group (i.e. any keeper that absorbed a loser).
   - For each tombstoned item: `store.setRecord(GroceryCodec.makeRecord(tombstoned_item, zoneID: zoneID))` then `.saveRecord(...)`. The tombstone (isUserRemoved=true) MUST be saved as an update, NOT deleted. Never call `store.removeRecord` or `.deleteRecord` for a dedupe loser.
   - For each repointedLink: `store.setRecord(EventGroceryItemCodec.makeRecord(link, zoneID: zoneID))` then `.saveRecord(...)`.
6. Decode all `"WeekMeal"` records where `record["week"] as? CKRecord.Reference` points to weekName → `[WeekMeal]`. Run `ConflictRepair.repairDuplicateSlots(meals:, slots: knownSlots)`. Run `ConflictRepair.reconcileSortOrder(_:)`. Enqueue `.saveRecord` for any WeekMeal whose fields changed.

Also run `ConflictRepair.collapseDuplicateWeeks` once per batch (not per-week): decode all `"Week"` records from the store → `[Week]`. For each `WeekCollapse` result (keeper + losers), re-parent all children (set their `weekRef` CKReference to keeper's recordID) and enqueue saves; then enqueue `.deleteRecord` for each loser Week.

**Known slot vocabulary**: the slot names must come from somewhere. Read the current WeekMeal prod model (`app/models/week.py`) to find the slot vocabulary, then either hardcode it in the engine as a constant or pass it at engine-init time. Find the existing slot vocabulary in the prod codebase before prescribing anything here.

**Threading**: `syncEngine.state.add(pendingRecordZoneChanges:)` is safe to call inside a delegate callback per CKSyncEngine docs — it buffers.

**Verify:** add a headless test in `GroceryMergeTests/ConflictRepairTests.swift` (already exists): `dedupeGrocery` re-run idempotency test — run once over a set with one duplicate, then run again over the resulting keepers+tombstoned, assert the second run produces zero new tombstoned items. This tests the tombstone-filter at line 32 of ConflictRepair.swift works correctly.

---

## Work item 6: EventMergeEngine (pure, CloudKit-free, headless-unit-testable)

**What to read first:** `app/services/event_grocery.py` lines 34-525 (the full service; read every function). `app/models/event.py:213-254` and `app/models/week.py:25-30`. `SimmerSmithCloudKit/Sources/GroceryMerge/ConflictRepair.swift` (the pure-function module style to mirror). `SimmerSmithCloudKit/Sources/GroceryMerge/Models.swift` (after Work item 1 expansion).

**Create:** `SimmerSmithCloudKit/Sources/GroceryMerge/EventMergeEngine.swift`

Pure Swift, no CloudKit import. Mirrors the ConflictRepair pattern: static functions (or a struct with no state) operating over in-memory collections of value types, returning result structs.

**Result struct:**
```
struct EventMergeResult {
    var changedGroceryItems: [GroceryItem]       // keepers updated with new eventQuantity
    var newGroceryItems: [GroceryItem]           // created by no-match branch
    var deletedGroceryItemNames: [String]        // event-only rows self-deleted by unmerge
    var changedEventLinks: [EventGroceryItem]    // merged_into_* pointers updated
    var updatedEvent: Event                      // linkedWeekID updated
}
```

**Functions to port — read event_grocery.py line-by-line for each, DO NOT transcribe from this prompt:**

`_matchKeys(evRow: EventGroceryItem) -> (baseKey: String, nameKey: String)`: port event_grocery.py:285-306 verbatim. The baseKey uses `baseIngredientID + unit` if baseIngredientID is set, else `normalizedName + unit`. The nameKey is always `normalizedName + unit`. Document in a comment that this is coarser than `Models.MergeKey` and serves a different purpose (event→week matching, not CKSyncEngine conflict resolution). Do NOT reuse `mergeKey`.

`mergeEventIntoWeek(event: Event, week: Week, eventLinks: [EventGroceryItem], weekGroceries: [GroceryItem]) -> EventMergeResult`: port event_grocery.py:309-393.
- Build the week index keyed by BOTH matchKey results per GroceryItem (same dual-key approach as _match_keys — find the week-index-building pattern at event_grocery.py:328-335 and mirror it).
- Idempotency guard per ev_row: if `ev_row.mergedIntoWeekID == week.recordName && ev_row.mergedIntoGroceryItemID != nil` → skip.
- Text-only rows (`ev_row.totalQuantity == nil`): set `mergedIntoWeekID = week.recordName` only, no numeric add.
- Match: `match.eventQuantity = round((match.eventQuantity ?? 0) + ev_row.totalQuantity, 2)` (round to 2dp, mirror server). Set both pointers.
- No-match: create a new GroceryItem with `totalQuantity: nil`, `eventQuantity: ev_row.totalQuantity`, `sourceMeals: "event:\(event.name)"` (the T4 name-agnostic marker), all other fields from ev_row. Set both pointers on ev_row. Add the new item to the week index under both keys.
- Set `updatedEvent.linkedWeekID = week.recordName`.

`unmergeEventFromWeek(event: Event, week: Week, eventLinks: [EventGroceryItem], weekGroceries: [GroceryItem], keepLink: Bool) -> EventMergeResult`: port event_grocery.py:475-525.
- For each ev_row where `mergedIntoWeekID == week.recordName`:
  - Lookup target = weekGroceries.first { $0.recordName == ev_row.mergedIntoGroceryItemID }
  - **NIL-TARGET GUARD**: if target is nil or ev_row.totalQuantity is nil → skip subtraction, still clear pointers. This is the cross-aggregate atomicity race guard (event_grocery.py:493).
  - `newEventQty = round(target.eventQuantity ?? 0 - ev_row.totalQuantity, 2)`; `target.eventQuantity = newEventQty > 0.0001 ? newEventQty : nil` (the 0.0001 threshold, port exactly).
  - Self-delete decision (ALL three must hold): `isEventOnly = target.sourceMeals.hasPrefix("event:")` (prefix check, NOT equality — T4 name-agnostic); `noRemainingQty = (target.totalQuantity ?? 0) <= 0 && (target.eventQuantity ?? 0) <= 0`; `noUserInvestment = target.quantityOverride == nil && target.unitOverride == nil && target.notesOverride == nil && !target.check.isChecked && !target.isUserAdded`. If all three → add target.recordName to `deletedGroceryItemNames`.
  - Clear ev_row pointers.
  - If not keepLink and `event.linkedWeekID == week.recordName`: `updatedEvent.linkedWeekID = nil`.

`resolveTargetWeek(event: Event, weeks: [Week]) -> Week?`: port event_grocery.py:396-423.
- If `event.linkedWeekID` is set → return `weeks.first { $0.recordName == event.linkedWeekID }`.
- If `event.eventDate == nil` → return nil.
- Otherwise: filter weeks where `weekStart <= eventDate && weekEnd >= eventDate` (String lexical comparison — valid only for ISO-8601 YYYY-MM-DD), sort by `weekStart` descending, return first (M71 tiebreak: latest-starting covering week).
- Assert in a comment that the lexical comparison is valid only when all date strings are YYYY-MM-DD (enforced at codec boundary by EventCodec).

`applyAutoMergePolicy(event: Event, weeks: [Week], eventLinks: [EventGroceryItem], allWeekGroceries: [String: [GroceryItem]]) -> EventMergeResult`: port event_grocery.py:426-472.
- Param `allWeekGroceries` is keyed by weekRecordName.
- **Branch 1 — manually_merged pin** (event_grocery.py:449-454): if `event.manuallyMerged`, AND `event.linkedWeekID` is set AND the linked week is found → call `mergeEventIntoWeek` into that week, return. Never auto-unmerge.
- **Branch 2 — auto_merge_grocery** (event_grocery.py:456-465): if `event.autoMergeGrocery`, AND linkedWeekID is set AND eventDate is non-nil AND the event_date moved off the linked week's [start,end] range → `unmergeEventFromWeek(keepLink: false)`, then `resolveTargetWeek`, then `mergeEventIntoWeek` into the resolved week.
- **Branch 3 — neither** (event_grocery.py:468-472): if linkedWeekID is set → `unmergeEventFromWeek(keepLink: false)`.

**Verify:** `swift test --filter EventMergeEngineTests` — all pass. Tests to write in a new file `GroceryMergeTests/EventMergeEngineTests.swift` (mirror ConflictRepairTests.swift style with `@Test` and `#expect`):
- `3→6→9 guard`: merge (eventQuantity=3), then unmerge(keepLink:true) then rebuild ev_row with same quantity then merge again → eventQuantity==3 still, not 6.
- `unmergeToZeroSelfDeletes`: event-only GroceryItem (sourceMeals starts with "event:", no investment) → unmerge → appears in deletedGroceryItemNames.
- `unmergeWithCheckDoesNotDelete`: same but check.isChecked=true → NOT in deletedGroceryItemNames.
- `textOnlyMerge`: ev_row with totalQuantity=nil → merged_into_week_id set, eventQuantity on GroceryItem unchanged.
- `matchKeysDualFallback`: catalog-resolved ev_row (has baseIngredientID) matches week grocery with same baseIngredientID; name-only ev_row matches by normalizedName+unit.
- `manualMergePinShortCircuit`: event.manuallyMerged=true, event.eventDate moves to a different week → still merges into linkedWeekID, NOT the new covering week.
- `redateUnmergesThenResolves`: auto_merge_grocery=true, event_date moves off linked week → result includes both unmerge of old week and merge into new covering week.
- `m71LatestWeekTiebreak`: two weeks both covering the event_date → result uses the one with the later weekStart.
- `nilTargetGuard`: ev_row.mergedIntoGroceryItemID points at a recordName that is NOT in weekGroceries → unmerge completes without crash, pointers cleared.

---

## Work item 7: Sim DEBUG panel check (CloudKitDebugView.swift)

**What to read first:** `SimmerSmith/SimmerSmith/Features/Settings/CloudKitDebugView.swift` — find the `runHouseholdSyncCheck` function and the `runPhase4Check` function (if it exists from the Phase 4 main landing). Mirror the two-engine-on-one-zone pattern exactly (separate temp stateURLs, `automaticSync: false`, manual `sync()` + `sendUntilDrained`).

Add a `runPhase4RemainderCheck()` function in `#if DEBUG`:

1. Install a `CompositeMerger([GrocerySyncMerger(), EventGrocerySyncMerger(), EventSyncMerger()])` on both engines.
2. **Sticky Event pin**: engineA creates an Event with `manuallyMerged = true`; engineB concurrently edits `eventDate`; after both sync, assert `manuallyMerged == true` on both engines.
3. **EventGroceryItem pointer merge**: engineA creates an EventGroceryItem with `mergedIntoGroceryItemID = "G1"`; engineB concurrently sets it to nil (unmerge in flight); after sync, assert the `preferLive` semantics — the live pointer wins over the concurrent nil if engineA is the later writer, or the nil wins if engineB is later. The key assertion is that the merge runs (not a blanket LWW that blindly takes the remote).
4. **Post-batch dedupe**: engineA creates two GroceryItems with identical (normalizedName, unit) on the same week; engineB fetches → after the repair pass runs, engineB should see only one live row (the other tombstoned). Assert `isUserRemoved == true` on the loser, not that it was deleted.
5. Print per-step ✅/❌ trace.

**Note on transient dangling pointers**: the sim panel comment must note that EventGroceryItem records arrive before their target GroceryItem in CloudKit (no transactional grouping). The poll-until-converged helper must drain until BOTH records are present before asserting. A transiently-dangling pointer is expected behavior, not a failure.

**valueTypeChanges:**
- EventGroceryItem: add 12 fields — totalQuantity:Double?, baseIngredientID:String?, ingredientVariationID:String?, ingredientName:String='', normalizedName:String='', unit:String='', quantityText:String='', category:String='', sourceMeals:String='', notes:String='', reviewFlag:String='', resolutionStatus:String='unresolved'. All defaulted. Existing tests unchanged.
- Week: add weekEnd:String=''. Existing passthroughLWW test unchanged.
- Event: change eventDate:String to eventDate:String? (nil=no date). The existing ResolverTests.swift:67 Event init with eventDate:'2026-07-04' still compiles (String literal is String?). FieldMergeResolver.merge(Event) does not read eventDate; no resolver change needed.
- GroceryItem: add storeLabel:String=''. Pass-through only; no merge logic, no new resolver branch.

**invariants:**
- EventGroceryItem.totalQuantity (Swift) = prod total_quantity (Python/ORM) = the event row's own aggregated contribution. It is NEVER the same as GroceryItem.eventQuantity (which accumulates contributions from multiple events). The codec CKRecord field name must be 'totalQuantity' to match the CKDSL block.
- mergedIntoWeekID and mergedIntoGroceryItemID on EventGroceryItem MUST be encoded as plain String keys in the CKRecord, NOT as CKReference. Using CKReference for these SET-NULL soft refs would violate §C and break nullingDangling.
- dedupeGrocery losers MUST be saved as tombstones (isUserRemoved=true) not hard-deleted. Hard deletion lets a peer re-create the row (no tombstone to suppress it), reproducing the build-45 exponential-dupe. The .saveRecord enqueue for tombstoned items is mandatory.
- The 0.0001 threshold in unmerge (newEventQty > 0.0001 → keep, else nil) is load-bearing: floating-point subtraction can produce small positive residuals. Port exactly, do not round or change to 0.
- The 'event:' prefix check in unmerge self-delete is a PREFIX check (.hasPrefix('event:')), NOT equality with the current event name. A rename after merge would strand zombie rows if equality were used.
- regenerate_event_grocery must call unmerge with keepLink=true BEFORE the bulk delete of old ev_rows. Without this, the re-merge adds the new contribution on top of the stale one, producing 3→6→9 double-counting.
- All three date String fields (Event.eventDate, Week.weekStart, Week.weekEnd) MUST be ISO-8601 YYYY-MM-DD in CloudKit and the Swift value type. Lexical ordering == date ordering ONLY for zero-padded YYYY-MM-DD. The EventCodec is the sole enforcement point and must use ISO8601DateFormatter with .withFullDate.
- The _matchKeys function used inside EventMergeEngine is coarser than Models.MergeKey (no locked-variation, no quantityText) and serves a different purpose (event→week row matching). They MUST NOT be conflated or reused for each other's purpose.
- The nil-target guard in unmergeEventFromWeek (if target == nil → skip subtraction, still clear pointers) is mandatory for correctness under CloudKit's non-transactional per-record delivery. EventGroceryItem can arrive before its GroceryItem target.
- CompositeMerger must be installed on the engine before any sync that involves Event/EventGroceryItem records. An engine with merger=nil silently falls through to blanket LWW for those types, dropping the manuallyMerged pin.

**headlessTests:**
- swift test --package-path /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit — must run all 52+ existing tests green after Models.swift expansion (Work item 1), confirming additive defaults preserved all existing call sites
- swift test --package-path /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit --filter GroceryMergeTests — EventMergeEngineTests must pass: 3→6→9 guard, self-delete with/without investment, text-only merge, dual matchKey fallback, manuallyMerged pin short-circuit, re-date unmerge, M71 tiebreak, nil-target guard (9 tests)
- swift test --package-path /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit --filter HouseholdSyncTests — EventGroceryItemCodec round-trip (all 16 fields), EventCodec round-trip (nil and non-nil eventDate), CompositeMerger dispatch, EventSyncMerger pin test (4 new tests)
- swift test --package-path /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit --filter ConflictRepairTests — dedupeGrocery idempotency re-run test (tombstone filter prevents double-count on second pass)
- swift build --package-path /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit — confirms EventGroceryCodecs.swift + EventSyncMergers.swift compile under Swift 5 mode with CloudKit import guard
- User runs: xcrun cktool validate-schema --team-id <TEAM_ID> --container-id iCloud.app.simmersmith.cloud --environment development --file /Users/tfinklea/git/simmersmith/.docs/ai/phases/phase0-schema.ckdb (human-verified; gates the sim check)

**risks:**
- IRREVERSIBLE CKDSL: Field names, types, and the REFERENCE vs STRING distinction for mergedIntoWeekID/mergedIntoGroceryItemID on EventGroceryItem cannot change once deployed to Production. Lock these before any Production push. Adding fields is safe; renaming or retyping is not.
- IRREVERSIBLE CKDSL: Adding weekRef REFERENCE (CASCADE) to GroceryItem and week REFERENCE (CASCADE) to WeekMeal means deleting a Week will cascade locally on the deleting device. Confirm this is the intended behavior per §C before deploying to Production.
- The EventCode eventDate codec maps String? to TIMESTAMP and back. If any code path writes eventDate to a CKRecord without going through EventCodec (e.g. a direct record['eventDate'] = someValue bypass), the YYYY-MM-DD invariant is silently broken and _resolve_target_week produces wrong results.
- The Event CKDSL block already deployed to DEV has eventDate as TIMESTAMP SORTABLE. The Swift value type is now String?. The codec must do the Date↔String conversion at every encode/decode boundary. If the codec is omitted from a code path, the raw TIMESTAMP from the CKRecord (a Date object) will crash when assigned to String?.
- collapseDuplicateWeeks re-parents children of loser Weeks by updating their weekRef CKReference. This requires writing new CKRecords for all children (potentially many GroceryItems and WeekMeals). The re-parent writes + loser deletes are separate pending changes that can partial-fail; the repair pass must be re-runnable so a partial fail on first run is corrected on the next batch trigger.
- The post-batch repair pass calls store.allRecords() for every affected week. At household scale (hundreds of records) this is acceptable; at larger scales it becomes a bottleneck. Document as a known O(N) scan; the secondary index optimization (Work item 5 Option B) is deferred to a future phase.
- The FieldMergeResolver.merge(EventGroceryItem) uses the current (thin) value type; after Work item 1 expands it, the resolver must still compile and pass its existing test (ResolverTests.swift:51-60). The test constructs EventGroceryItem with only the original 5 fields — the new fields default to empty/nil, so the test is additive-safe, but verify it.

**filesToCreate:**
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Sources/HouseholdSync/EventGroceryCodecs.swift — EventGroceryItemCodec + EventCodec — CKRecord encode/decode for EventGroceryItem and Event, behind #if canImport(CloudKit). Mirror GroceryCodec exactly (recordType, makeRecord, encode(into:), decode). EventCodec maps String? eventDate ↔ CKDSL TIMESTAMP via ISO8601DateFormatter(.withFullDate), enforcing the YYYY-MM-DD lexical-ordering invariant. mergedIntoWeekID/mergedIntoGroceryItemID/linkedWeekID as plain String keys (not CKReference). Clocks as INT64 SyncClock.
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Sources/HouseholdSync/EventSyncMergers.swift — EventGrocerySyncMerger + EventSyncMerger conforming to RecordMerger, plus CompositeMerger that dispatches to the first matching handler. Mirror GrocerySyncMerger exactly. CompositeMerger is the public type the caller installs on engine.merger to handle all three sticky record types.
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Sources/GroceryMerge/EventMergeEngine.swift — Pure CloudKit-free port of _match_keys / merge_event_into_week / unmerge_event_from_week / _resolve_target_week / apply_auto_merge_policy from app/services/event_grocery.py:285-525. Returns EventMergeResult (changedGroceryItems, newGroceryItems, deletedGroceryItemNames, changedEventLinks, updatedEvent). Headless-unit-testable; mirrors ConflictRepair module style.
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Tests/GroceryMergeTests/EventMergeEngineTests.swift — Swift Testing suite for EventMergeEngine covering: 3→6→9 double-count guard, self-delete with/without user investment, text-only merge, dual matchKey fallback, manuallyMerged pin short-circuit, re-date unmerge+re-resolve, M71 latest-week tiebreak, nil-target guard in unmerge.

**filesToModify:**
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Sources/GroceryMerge/Models.swift — Additive expansions only (all with defaults to preserve existing call sites): (1) EventGroceryItem: add totalQuantity:Double?, baseIngredientID:String?, ingredientVariationID:String?, ingredientName:String='', normalizedName:String='', unit:String='', quantityText:String='', category:String='', sourceMeals:String='', notes:String='', reviewFlag:String='', resolutionStatus:String='unresolved'. (2) Week: add weekEnd:String='' before modifiedAt. (3) Event: change eventDate:String to eventDate:String? with default nil; update init; add comment that nil = no date, non-nil MUST be YYYY-MM-DD. (4) GroceryItem: add storeLabel:String='' (pass-through, no merge logic).
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Sources/HouseholdSync/HouseholdSyncEngine.swift — After the per-modification loop in .fetchedRecordZoneChanges handler (around line 174): add a post-batch repair pass. Collect affectedWeekNames from the batch (GroceryItem weekRef + EventGroceryItem mergedIntoWeekID). For each affected week: decode-all-and-filter for GroceryItems + EventGroceryItems; run ConflictRepair.dedupeGrocery; enqueue .saveRecord for keepers+tombstoned+repointedLinks (tombstoned = save with isUserRemoved=true, NOT deleteRecord). Run repairDuplicateSlots+reconcileSortOrder over WeekMeals for the same week. Run collapseDuplicateWeeks over all Week records once per batch; re-parent children of loser weeks onto keepers.
- /Users/tfinklea/git/simmersmith/.docs/ai/phases/phase0-schema.ckdb — Four additive changes: (1) Append new RECORD TYPE blocks after line 338: Week (weekStart STRING QUERYABLE SORTABLE, weekEnd STRING, status/notes/readyForAIAt/approvedAt/pricedAt/createdAt/updatedAt), WeekMeal (dayName/slot/sortOrder INT64/aiGenerated INT64/approved INT64/notes/createdAt/updatedAt, week REFERENCE), EventGroceryItem (all 16 fields per Work item 4 spec, event REFERENCE, mergedIntoWeekID/mergedIntoGroceryItemID as plain STRING). (2) Add modifiedAtClock INT64 to existing Event block (additive field). (3) Add weekRef REFERENCE + createdAtTimestamp TIMESTAMP SORTABLE + storeLabel STRING to existing GroceryItem block (additive fields). User must validate+deploy to DEV with management token before the sim check.
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Tests/GroceryMergeTests/ConflictRepairTests.swift — Add one idempotency test: run dedupeGrocery once over a set with one duplicate pair, then run again over the resulting keepers+tombstoned set; assert the second run produces zero new tombstoned items (proves the isUserRemoved filter at line 32 prevents double-counting on re-run).
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Tests/HouseholdSyncTests/GroceryMergeSeamTests.swift — Add: (1) EventGroceryItemCodec round-trip test — all 16 fields survive makeRecord+decode including nil totalQuantity and non-nil totalQuantity. (2) EventCodec round-trip test — non-nil eventDate='2026-07-04' encodes as TIMESTAMP and decodes back to '2026-07-04'; nil eventDate round-trips to nil. (3) CompositeMerger test — handles returns true for all three record types, false for 'Recipe'. (4) EventSyncMerger manuallyMerged pin test mirroring mergerPreservesLocalCheckOverLaterRemoteAuto.
- /Users/tfinklea/git/simmersmith/SimmerSmith/SimmerSmith/Features/Settings/CloudKitDebugView.swift — Add runPhase4RemainderCheck() in #if DEBUG block: installs CompositeMerger on both engines, runs 4 sim checks (sticky merger for all 3 types, post-batch dedupe tombstone not delete, nil-target guard, week collapse re-parent). Print per-step ✅/❌ trace mirroring the existing Phase 4 main check.

**simVerify:** Two-engine-on-one-shared-zone pattern (mirror runHouseholdSyncCheck in CloudKitDebugView.swift): separate temp stateURLs, automaticSync=false, manual sync()+sendUntilDrained. Four checks:

1. CompositeMerger handles all three record types (GroceryItem / EventGroceryItem / Event). Install it on both engines; create one record of each type on engineA; engineB fetches; assert each was field-merged not blanket-LWW-overwritten by verifying a sticky field (manuallyMerged on Event, mergedIntoGroceryItemID on EventGroceryItem, isUserRemoved on GroceryItem) survived a concurrent conflicting write from engineA.

2. Post-batch dedupe convergence: engineA creates two GroceryItems with identical (normalizedName='chicken', unit='lb') on a test Week; engineB syncs; assert engineB's store has exactly one GroceryItem with isUserRemoved=false and one with isUserRemoved=true (the tombstone). The loser must be SAVED (appears in store) not deleted (absent from store).

3. EventGroceryItem nil-target guard: engineA creates an EventGroceryItem with mergedIntoGroceryItemID pointing at a recordName that does NOT exist in engineB's store yet; engineB fetches the EventGroceryItem record; assert no crash, and the pointer is preserved (the target will arrive on a later fetch).

4. Week collapse: two engines simultaneously create a Week record with weekStart='2026-07-07' (same weekStart, different recordNames via PKs); after both sync, exactly one Week with weekStart='2026-07-07' remains live (the keeper = lower recordName per collapseDuplicateWeeks) and all GroceryItem/WeekMeal children have been re-parented onto it. This exercises the collapseDuplicateWeeks repair pass.

Print ✅/❌ per step.

### Review — prod-fidelity (approved=False)

- **[critical]** Self-delete in unmergeEventFromWeek is a HARD DELETE in prod, but the blueprint conflates it with dedupe tombstoning. app/services/event_grocery.py:519 does `session.delete(target)` — a real SQL DELETE of the GroceryItem row. The blueprint result struct `deletedGroceryItemNames` signals hard deletion correctly, but Work item 5 post-batch repair wiring says 'Never call store.removeRecord or .deleteRecord for a dedupe loser' without distinguishing that dedupeGrocery losers must be tombstoned while unmerge self-deletes must be hard-deleted (store.removeRecord + .deleteRecord). The invariants section reinforces tombstone-only for ALL cases, which is wrong for the unmerge path. An implementer reading both Work item 5 and the invariants section will tombstone self-deletes instead of hard-deleting them, producing zombie event-only rows that accumulate. — *FIX:* Add a clear distinction: ConflictRepair.dedupeGrocery losers MUST be tombstoned (isUserRemoved=true, .saveRecord). EventMergeEngine.unmergeEventFromWeek self-deletes (entries in deletedGroceryItemNames) MUST be hard-deleted (store.removeRecord + .deleteRecord) — this mirrors prod session.delete(target) at event_grocery.py:519. Update the invariants section to reflect that only dedupe losers are tombstoned; unmerge self-deletes are true hard deletes.
- **[critical]** applyAutoMergePolicy Branch 2 condition is wrong. Blueprint says Branch 2 (auto_merge_grocery) calls mergeEventIntoWeek only when 'eventDate is non-nil AND the event_date moved off the linked week's range'. Prod (event_grocery.py:456-465): the unmerge is gated on `event.linked_week_id and event.event_date is not None and linked is not None and not (range check)`, but then `_resolve_target_week` and `merge_event_into_week` run UNCONDITIONALLY regardless of whether the unmerge fired or whether eventDate is nil. The blueprint adds a spurious `AND eventDate is non-nil` gate around the always-run merge, which would break idempotent re-merge when an event has no date. — *FIX:* Port Branch 2 exactly as prod: (1) conditionally unmerge only if linkedWeekID is set AND eventDate is non-nil AND linked week is found AND date moved off range. (2) ALWAYS call resolveTargetWeek and if result is non-nil call mergeEventIntoWeek — no additional gate. The nil-eventDate path is covered by resolveTargetWeek returning nil, which prevents the merge.
- **[high]** EventGroceryItem.eventQuantity field does not exist on the prod ORM. The current Swift EventGroceryItem value type (Models.swift:99) has `eventQuantity: Double?`. But app/models/event.py EventGroceryItem has no `event_quantity` column — only `total_quantity`. The field `eventQuantity` on the Swift type appears to have been incorrectly added to mirror GroceryItem.eventQuantity. Work item 1 proposes adding `totalQuantity: Double?` as a NEW field with the comment that it maps to prod `total_quantity`. But the existing `eventQuantity` field on the Swift type is already wired into FieldMergeResolver.merge(EventGroceryItem) and the ConflictRepair test at ResolverTests.swift:51-60. The blueprint needs to explicitly address what `eventQuantity` on the Swift type represents (it was placed there to track the event row's own contribution through the merge resolver, parallel to GroceryItem.eventQuantity) and whether it should be renamed or removed now that `totalQuantity` is being added. — *FIX:* Clarify that the existing Swift EventGroceryItem.eventQuantity was a stand-in for tracking the event row's own contribution during CKSync conflict resolution, serving the same role as GroceryItem.eventQuantity (preferLive semantics in the merger). The new totalQuantity maps to prod total_quantity. These are two different fields: totalQuantity is the event row's aggregated contribution that gets added to the week's eventQuantity; eventQuantity on the Swift type is the CKSync-merge-preserved value. Rename the Swift field or add totalQuantity as a distinct field and update FieldMergeResolver accordingly. Document explicitly.
- **[high]** _matchKeys base_key structure mismatch. Blueprint says 'The baseKey uses baseIngredientID + unit if baseIngredientID is set'. This implies string concatenation. But prod (event_grocery.py:304): `base_key = (base, unit, '') if base else ('', unit, norm)` — a 3-tuple with the base_ingredient_id in the first slot, unit in second, empty string in third. String concatenation collides when baseIngredientID ends with the start of unit (e.g., 'chicken' + 'lb' == 'chickenl' + 'b'). The Swift implementation must use a struct or multi-field tuple identical to the Python 3-tuple structure, not concatenation. — *FIX:* Define a MatchKey struct (distinct from MergeKey) with fields (base: String, unit: String, qualifier: String). baseKey = base non-empty ? MatchKey(base: baseIngredientID, unit: unit, qualifier: '') : MatchKey(base: '', unit: unit, qualifier: normalizedName). nameKey = MatchKey(base: normalizedName, unit: unit, qualifier: ''). The week index keyed by MatchKey must use the same struct for both ev_row lookup and week_row indexing.
- **[high]** isEventOnly predicate in unmerge self-delete check uses wrong source. The existing GroceryItem.isEventOnly computed property (Models.swift:86-89) requires BOTH `eventQuantity > 0` AND `sourceMeals.hasPrefix('event:')`. But the prod self-delete check (event_grocery.py:509) is ONLY `(target.source_meals or '').startswith('event:')` — no eventQuantity guard. The blueprint says 'isEventOnly = target.sourceMeals.hasPrefix("event:")' (prefix check only, correct). But if the implementer uses the computed property `target.isEventOnly` from Models.swift, they introduce an incorrect `eventQuantity > 0` gate that prevents deletion of text-only event-only rows (where eventQuantity is nil after unmerge). — *FIX:* In EventMergeEngine.unmergeEventFromWeek, perform the event-only check as a direct sourceMeals prefix check (target.sourceMeals.hasPrefix('event:')), NOT by using the GroceryItem.isEventOnly computed property. Add a comment explaining this divergence from the computed property.
- **[medium]** Blueprint omits the notes-strip step from unmergeEventFromWeek. Prod (event_grocery.py:499-503) strips legacy '+event: {event.name}' note fragments from target.notes during every unmerge. This is the T4 fix #37 (name-agnostic detection migrated from mutable notes to source_meals prefix, but the note-strip for pre-M22.2 data is still present). The blueprint's EventMergeEngine.unmergeEventFromWeek spec never mentions this notes-strip operation. — *FIX:* Add to the unmergeEventFromWeek spec: after computing new_event_qty, strip any note parts containing '+event:' from target.notes (join remaining parts with '; '). Mirror event_grocery.py:499-503 exactly. This is a no-op for post-M22.2 data but required for backward compatibility with old merged rows.
- **[medium]** Event.eventDate nilability change (String to String?) collides with existing code that uses empty string as 'no date'. The current Event struct (Models.swift:145) has `eventDate: String = ''` as a default. Changing to `eventDate: String? = nil` changes the semantics of ALL existing Event constructors that omit eventDate — they currently get '' (empty string) and will now get nil. The blueprint claims this is additive-safe because the ResolverTests.swift:67 test passes a literal. But any code that tests `eventDate.isEmpty` vs nil-check will differ. The existing FieldMergeResolver.merge(Event) ignores eventDate, so that is safe. However, GroceryItem.isEventOnly and any sort/filter code comparing eventDate to '' will break. — *FIX:* Audit for all existing Swift call sites that construct Event() without eventDate or that check eventDate == ''. Confirm none exist outside the test file before landing the type change. If any exist, add a bridging computed property or migration step. The codec enforces YYYY-MM-DD; the value type must align.
- **[medium]** Work item 5 slot vocabulary is left as a homework exercise but is fully determined in prod. Blueprint: 'read the current WeekMeal prod model to find the slot vocabulary'. The vocabulary is already known: app/services/weeks.py:11 defines SLOT_ORDER = {'breakfast': 0, 'lunch': 1, 'dinner': 2, 'snack': 3}. Leaving this as an exercise risks an implementer picking a wrong or incomplete list. — *FIX:* Lock the slot vocabulary in the blueprint: ['breakfast', 'lunch', 'dinner', 'snack'] from app/services/weeks.py:11 SLOT_ORDER. Hardcode this as a constant in the engine (e.g., `static let knownSlots = ["breakfast", "lunch", "dinner", "snack"]`) and pass it to repairDuplicateSlots.
- **[medium]** applyAutoMergePolicy Branch 3 in prod fires only when linkedWeekID is NOT nil. Blueprint says 'if linkedWeekID is set → unmergeEventFromWeek(keepLink: false)'. Prod (event_grocery.py:468-472): `if event.linked_week_id is None: return; week = _week(...); if week is not None: unmerge`. The blueprint omits the `week is not None` guard — if the week was deleted server-side (SET NULL FK), `_week(linkedWeekID)` returns None and the unmerge is skipped. Without this guard, an implementer who passes nil-week to unmerge will crash or produce undefined behavior. — *FIX:* Add the nil-week guard to Branch 3: look up the week by linkedWeekID; if the week is not found (nil), skip the unmerge and return (the link will be cleared by nullingDangling in the repair pass). Mirror event_grocery.py:470-472 exactly.
- **[low]** Work item 2 EventGroceryItemCodec parent reference guidance is incomplete. Blueprint says 'Mirror how RecipeIngredient's parent ref is set in the HouseholdRecordCodec'. But HouseholdRecordCodec uses the HouseholdRecordType manifest enum to look up the parent field name. EventGroceryItemCodec is a standalone enum outside HouseholdRecords — no manifest lookup is available to it. The implementer has no concrete reference for how to set the event REFERENCE. — *FIX:* State explicitly: in the creation path (not the codec), set `record['event'] = CKRecord.Reference(recordID: CKRecord.ID(recordName: eventRecordName, zoneID: zoneID), action: .deleteSelf)` before calling engine.save(record). The codec itself does not set the parent ref. Document this as the standard pattern for all CASCADE-child codecs outside HouseholdRecords.
- **[low]** Blueprint Work item 1 claims EventGroceryItem field count is 11 new fields but lists 12. The spec header says 'add 11 fields' but then lists: totalQuantity, baseIngredientID, ingredientVariationID, ingredientName, normalizedName, unit, quantityText, category, sourceMeals, notes, reviewFlag, resolutionStatus — that is 12 fields. Minor but confusing for implementers tracking the expansion. — *FIX:* Correct the count to 12 new fields.
- **[low]** The 3-6-9 double-count guard test in Work item 6 is specified as: 'merge, then unmerge(keepLink:true) then rebuild ev_row with same quantity then merge again → eventQuantity==3 still'. But in the pure EventMergeEngine, unmerge with keepLink:true still clears the ev_row's mergedIntoWeekID and mergedIntoGroceryItemID pointers (that is the whole point of unmerge). Rebuilding ev_row with the same quantity and merging again is NOT 3→6 because the pointer was cleared. The 3→6→9 bug requires NOT calling unmerge before the second merge — i.e., calling merge while the ev_row still has mergedIntoWeekID set. The test as specified would pass trivially because unmerge cleared the pointer and the second merge starts clean. The real guard is the idempotency check: `if ev_row.mergedIntoWeekID == week.recordName && ev_row.mergedIntoGroceryItemID != nil → skip` (event_grocery.py:340-341). — *FIX:* Rewrite the 3-6-9 test: (1) merge ev_row (eventQuantity on target = 3). (2) WITHOUT calling unmerge, call mergeEventIntoWeek again with the SAME ev_row (which still has mergedIntoWeekID and mergedIntoGroceryItemID set). Assert eventQuantity on target is still 3 (not 6). This tests the idempotency skip guard directly. The keepLink:true/rebuild test in the spec actually tests regenerate_event_grocery semantics, which is a different (also important) invariant — keep it as a separate test named appropriately.

### Review — merge-invariants (approved=False)

- **[blocker]** isEventOnly computed property is wrong for unmerge self-delete test in EventMergeEngine. The existing Models.swift `GroceryItem.isEventOnly` (line 86-88) requires `eventQuantity > 0` AND `sourceMeals.hasPrefix("event:")`. But the production `_is_event_only` in grocery.py:354-360 is identical. The blueprint's unmergeEventFromWeek spec says `isEventOnly = target.sourceMeals.hasPrefix("event:")` — a PREFIX CHECK ONLY, no eventQuantity guard. After subtracting, eventQuantity may have dropped to nil/zero BEFORE the isEventOnly check runs, making `isEventOnly` return false and preventing the self-delete. The correct port reads `source_meals` independently of the post-subtraction `event_quantity`. Use a local check `target.sourceMeals.hasPrefix("event:")` directly in the function body, not the computed property, or the self-delete logic will silently fail for event-only rows that get unmerged. — *FIX:* In unmergeEventFromWeek, compute `let isEventOnly = target.sourceMeals.hasPrefix("event:")` BEFORE modifying target.eventQuantity (or as a raw string check), not by reading `target.isEventOnly` (which requires eventQuantity > 0, which will be nil after subtraction). The spec says prefix check, not the computed property. Add a test: event-only row with totalQuantity=5, after unmerge target.eventQuantity becomes nil, confirm it appears in deletedGroceryItemNames.
- **[blocker]** EventGroceryItem.eventQuantity field naming collision creates a silent semantic bug in FieldMergeResolver. The existing EventGroceryItem in Models.swift (line 99) has a field called `eventQuantity: Double?`. The Phase 5 prior review (blocker at line 481 in the blueprints file) flagged this: the production EventGroceryItem has `total_quantity` (the event row's own contribution), NOT `event_quantity`. The Swift model mislabels this as `eventQuantity`. Work item 1 adds a new `totalQuantity: Double?` field BUT does NOT remove or reconcile the old `eventQuantity: Double?` on EventGroceryItem. Now both exist. FieldMergeResolver.merge(EventGroceryItem) at line 54 still merges `eventQuantity` using mergeEventQuantity — but EventMergeEngine (Work item 6) will use `totalQuantity` for the actual merge contribution. Two parallel quantity fields on EventGroceryItem, one of which is never populated by the codec and one of which is never merged, creates silent data divergence across devices. — *FIX:* Decide: either (a) rename the existing `eventQuantity` on EventGroceryItem to `totalQuantity` (matches production — the field IS total_quantity on the server) and update FieldMergeResolver.merge(EventGroceryItem) and all tests accordingly, OR (b) document clearly that EventGroceryItem.eventQuantity is a legacy artifact that must be nil always (it has no server analog) and all merge/unmerge logic uses totalQuantity. Option (a) is correct. The FieldMergeResolver merge for EventGroceryItem should use totalQuantity (or remove the max-wins logic for a field that the server never uses). This is the single highest-risk field-naming decision and is irreversible once encoded in deployed CloudKit records.
- **[blocker]** Post-batch repair pass tombstone enqueue logic has a double-save race. Work item 5 says: enqueue .saveRecord for keepers 'only if the keeper absorbed a loser (i.e. appears in a group with count > 1)'. But `dedupeGrocery` always returns keepers from singleton groups too (line 56: `guard group.count > 1 else { keepers.append(group[0]); continue }`). The simplest-correct framing in the blueprint ('always enqueue saves for keepers that absorbed a loser') is correct but incomplete: tombstoned items MUST also be enqueued as saves even when the batch already caused a pending .saveRecord for that recordName. If both a .saveRecord (the incoming fetch that triggered the repair) and the repair's tombstone .saveRecord queue for the same tombstoned record, the engine may send the non-tombstoned version first and the tombstone second (correct), but may also interleave such that the re-save wins the server conflict and resurrects the record. The spec says tombstones are saved 'as updates', but the ordering of pendingRecordZoneChanges is not guaranteed when multiple .saveRecord entries exist for the same recordID. — *FIX:* After dedupeGrocery: for each tombstoned item, write the tombstoned CKRecord to the local store FIRST (store.setRecord), then enqueue .saveRecord. This ensures the local copy is the tombstone version regardless of what version was there before. Do NOT use store.removeRecord for losers at any point. Add the idempotency test the blueprint already specifies (re-run dedupeGrocery produces zero new tombstones) as the primary guard, but add a test that the store holds the tombstone version (isUserRemoved=true) after the pass runs, not the original live version.
- **[high]** apply_auto_merge_policy Branch 3 is mis-ported. The production code (event_grocery.py:468-472) only unmerges when `event.linked_week_id is NOT None` — if already unlinked, it's a no-op. The blueprint spec says 'if linkedWeekID is set → unmergeEventFromWeek(keepLink: false)'. That is correct. BUT Branch 2's auto-unmerge condition in the blueprint reads 'if event.autoMergeGrocery AND linkedWeekID is set AND eventDate is non-nil AND the event_date moved off the linked week'. The production code at line 457 checks `event.linked_week_id AND event.event_date is not None` (both must hold) before unmerging, then ALWAYS calls resolveTargetWeek+merge after (lines 463-465), even if no unmerge happened (event_date still on same week = idempotent merge). The blueprint correctly describes this, but the Swift port's branch ordering matters: Branch 2 must call resolveTargetWeek+mergeEventIntoWeek even when the unmerge was skipped (date not moved). Missing this produces a bug where auto_merge_grocery events with a correct linked week never get merged on a fresh device (no sync triggers a re-merge unless unmerge first fires). — *FIX:* In applyAutoMergePolicy Branch 2: always call resolveTargetWeek and mergeEventIntoWeek after the conditional unmerge, regardless of whether the unmerge fired. The unmerge is conditional; the re-merge is unconditional within this branch. Mirror event_grocery.py:463-465 exactly.
- **[high]** Week collapse (collapseDuplicateWeeks) re-parents children by hard-deleting the loser Week records, but the blueprint says re-parent first then delete losers. The CKRecord reference graph means GroceryItem.weekRef (a CASCADE .deleteSelf reference) on a loser's children will cascade-delete those children on the issuing device if the Week delete is enqueued BEFORE the re-parent saves go out. The blueprint notes 'the re-parent writes + loser deletes are separate pending changes that can partial-fail', but it does not specify ordering within the single batch. If .deleteRecord(loserWeekID) is enqueued before .saveRecord(re-parentedGroceryItem), CloudKit may reorder them during the send phase or the local .deleteSelf cascade may fire on re-parent before the save confirms. — *FIX:* Explicitly order: (1) enqueue ALL .saveRecord for re-parented children first, (2) THEN enqueue .deleteRecord for loser Week records. Document this ordering constraint in the engine code. Also note: HouseholdLocalStore.setRecord for re-parented children must happen before store.removeRecord for the loser Week, to prevent the local cascade scan from sweeping children that are about to be re-parented.
- **[high]** EventCodec TIMESTAMP encoding for eventDate is irreversible and the blueprint's approach breaks the nil-round-trip. The blueprint specifies `record["eventDate"] = nil` when eventDate is nil. CKRecord fields can be set to nil to clear them. However, the CKDSL defines `eventDate TIMESTAMP SORTABLE`. In CloudKit, once a SORTABLE field exists, a nil value may not sort correctly with non-nil values. More critically: the blueprint says the Event CKDSL block 'already exists' (phase0-schema.ckdb:187-201) and has `eventDate TIMESTAMP SORTABLE`. But `modifiedAtClock INT64` is NOT in the current schema — Work item 4 adds it additively. If an implementer runs the sim check BEFORE deploying the updated schema (with modifiedAtClock added), the EventCodec will write `record["modifiedAtClock"] = value` to a schema that doesn't have this field, and CloudKit will reject the save silently or with 'unknown field'. The blueprint's sim check depends on a schema deploy that requires a human step (cktool validate+import) — this ordering dependency is mentioned but not enforced as a hard gate. — *FIX:* Make the schema deploy (Work item 4 cktool validate+import to DEV) an explicit blocking gate before any Swift code that writes new fields to CKRecord. Add a guard in the sim debug panel: check that the Event record type has modifiedAtClock before running the Phase 4-remainder check, and print a ❌ with instructions to deploy the schema if absent.
- **[high]** The 3→6→9 double-count guard test (Work item 6) is described as: merge, unmerge(keepLink:true), rebuild ev_row with same quantity, merge again → eventQuantity==3. But this test does NOT exercise the production regenerate_event_grocery path (event_grocery.py:225-282), which does: (1) unmerge with keepLink:true, (2) bulk-delete ALL EventGroceryItem rows, (3) rebuild new EventGroceryItem rows, (4) re-merge. The test's 'rebuild ev_row' step in the test skips the bulk-delete. The actual 3→6→9 guard in prod (the T4 fix) works because the OLD ev_row's mergedIntoGroceryItemID is nil after the unmerge clears it (line 520-521), and then the bulk delete removes the row entirely, so the new ev_row starts fresh. In the Swift port, if EventMergeEngine is called after a partial-sync where old EventGroceryItem records still exist (mergedIntoGroceryItemID already set), the idempotency guard at the top of mergeEventIntoWeek (`ev_row.mergedIntoWeekID == week.recordName && ev_row.mergedIntoGroceryItemID != nil → skip`) would skip them. The 3→6→9 scenario in CloudKit is: old EventGroceryItem arrives on device B while device A already merged and re-merged. The idempotency guard protects correctly — but ONLY if the unmerge properly clears the pointers before any re-merge. — *FIX:* Add a test that more closely mirrors the CloudKit scenario: two EventGroceryItem records for the same event arrive in order (old one with pointer set, new one with pointer nil). Assert the merger (FieldMergeResolver.merge(EventGroceryItem)) picks the live pointer (old one wins via preferLive), then assert that if the sequence is unmerge-then-merge (pointers cleared then re-set), the final eventQuantity is exactly totalQuantity, not 2x. This exercises the CKSyncEngine race that the unit test misses.
- **[medium]** Work item 5 reads affectedWeekNames from GroceryItem.weekRef but weekRef does not yet exist in the GroceryItem Swift value type or GroceryCodec — it is added in Work item 4 (CKDSL) but the codec currently (GroceryCodec.swift) does not encode/decode a weekRef field. The post-batch repair pass must read `record["weekRef"] as? CKRecord.Reference` from the raw CKRecord to extract the weekName, but GroceryCodec.decode has no weekRef field. The blueprint's 'filesToModify GroceryCodec' is not listed under Work item 5 — it's implicitly required. Similarly, GroceryItem value type (Models.swift) has no weekID or weekRef property — the repair pass cannot get it from a decoded GroceryItem. Either GroceryItem needs a weekID field, or the repair pass must read weekRef directly from the raw CKRecord BEFORE decoding. — *FIX:* Add `weekID: String` (or `weekRef: String?`) to the GroceryItem value type (additive with default '') and encode/decode it in GroceryCodec as a plain String recordName (not CKReference for the value type — mirror how mergedIntoWeekID is a String on EventGroceryItem). The repair pass then reads groceryItem.weekID. Alternatively, read the weekRef CKReference from the raw CKRecord before decoding and pass it separately. Either way, this is a missing dependency that must be resolved before Work item 5 compiles.
- **[medium]** CompositeMerger installed on the engine after initial sync is too late. Work item 7 sim check says 'install CompositeMerger on both engines' then 'create one record of each type'. If the engine is initialized without a merger (merger=nil) and records sync before the merger is installed, the fetch handler's `if let merger` guard at HouseholdSyncEngine.swift:156 silently falls through to LWW for Event and EventGroceryItem records, dropping manuallyMerged pins permanently. In production, the merger must be installed BEFORE the first fetchChanges() call. The blueprint correctly states 'CompositeMerger must be installed on the engine before any sync' as an invariant, but Work item 3's engine wiring says 'the caller sets engine.merger = CompositeMerger(...)' with no enforcement. A forgotten install is silent data loss. — *FIX:* Make the merger non-optional or make HouseholdSyncEngine's init take a `merger: RecordMerger?` parameter that defaults to nil with a loud log warning if the engine fetches changes with merger=nil for a record type that has a known sticky semantic. Add an assertion in the sim debug panel that engine.merger is non-nil before starting the sticky-merge check steps.
- **[medium]** Event.eventDate type change from String to String? (Work item 1) changes existing resolver behavior silently. FieldMergeResolver.merge(Event) at line 76-83 does NOT read eventDate — confirmed. But the existing Event init at Models.swift:145 has `eventDate: String = ""`. After the change to String? with nil default, existing code that passes `eventDate: ""` (non-nil empty string) will still compile — but the EventCodec will encode this as `record["eventDate"] = formatter.date(from: "")` which returns nil (ISO8601DateFormatter fails on empty string), so `record["eventDate"] = nil`. This means a device running old code that writes `eventDate: ""` will encode nil to CloudKit, indistinguishable from a device running new code that writes `eventDate: nil`. That is fine for CloudKit. BUT the existing test at ResolverTests.swift:67 uses `eventDate: "2026-07-04"` — a non-nil String literal, which compiles as String? — correct. The risk is if any app-side code COMPARES `event.eventDate == ""` to detect 'no date', that comparison changes semantics after the nil migration (eventDate is now nil, not ""). The blueprint doesn't audit app-side call sites. — *FIX:* Before merging Work item 1, grep the full codebase (including the app target) for `eventDate == ""` or `eventDate.isEmpty` to find call sites that would silently break after the String→String? change. Replace those with `eventDate == nil`. Flag any app-side Event constructors that pass eventDate: "" — those now produce nil (via the codec) which is the correct semantic, but confirm they're intentional.
- **[medium]** dedupeGrocery tombstone enqueue in Work item 5 is specified as: 'only enqueue saves for keepers that have more than one item in their deduplication group'. But the GroceryDedupeResult.keepers list includes both group-size-1 keepers (no-op, should not be enqueued) and group-size-N keepers (absorbed losers, should be enqueued). The blueprint says 'compare decoded values' as the simplest approach but then says 'always enqueue saves for keepers that absorbed a loser'. These differ: a keeper that absorbed a loser may not actually have CHANGED values (e.g. if the loser had nil quantities), but must still be saved to ensure convergence. The safer approach is: always enqueue saves for keepers in groups where tombstoned count > 0 (i.e. group had losers). Never enqueue for singleton-group keepers (no change occurred). — *FIX:* Track per-group: if `group.count > 1` (at least one loser was tombstoned), always enqueue .saveRecord for the keeper (since its quantities/sourceMeals may have been updated). If `group.count == 1`, skip the keeper save — no change. This is already what the code structure implies (the `guard group.count > 1 else { keepers.append(group[0]); continue }` path doesn't modify the keeper).
- **[medium]** Work item 6's _matchKeys dual-key index has a key collision bug when base_ingredient_id is empty string vs nil. In event_grocery.py:301-304: `base = getattr(row, 'base_ingredient_id', None) or ''`. The `or ''` means a None base_ingredient_id becomes `''`. Then `base_key = (base, unit, '') if base else ('', unit, norm)` — so when base is `''`, it falls through to the `name_key` path. The Swift port must apply the same `nil-or-empty → use name fallback` logic. If the Swift implementation checks `if baseIngredientID != nil` (Swift Optional, not empty-string check), a row with `baseIngredientID = ""` (empty string, not nil) would incorrectly use the base-key path with an empty string, colliding with other empty-base-key rows. — *FIX:* In Swift _matchKeys, check `let base = baseIngredientID, !base.isEmpty` (non-nil AND non-empty) before using the base key path. Mirror the Python `or ''` coercion followed by the truthiness check. Add a test: ev_row with baseIngredientID = "" (empty, not nil) should match via normalizedName+unit, not via the empty base key.
- **[low]** EventGroceryItem codec: the blueprint says the CASCADE parent `event REFERENCE` is set separately at record-creation time (not by the codec), mirroring RecipeIngredient. But GroceryCodec.makeRecord does NOT set any parent reference — GroceryItem is a root record. The blueprint says 'find the RecipeIngredient parent-ref pattern and mirror it'. Reading HouseholdRecordCodec.swift shows the cascade parent is set as a CKReference at makeRecord time. The risk: if an implementer omits the event parent CKReference entirely (because the codec 'doesn't set it'), EventGroceryItem records will not cascade-delete when their Event is deleted. This is the same CASCADE semantics issue that the Phase 2b review identified for RecipeIngredient. — *FIX:* EventGroceryItemCodec.makeRecord must take an eventRecordID parameter and set `record["event"] = CKRecord.Reference(recordID: eventRecordID, action: .deleteSelf)`. Document this in the codec. The codec test (Work item 2 round-trip test) must also set+verify the event parent reference.
- **[low]** The slot vocabulary for repairDuplicateSlots is hardcoded as ["breakfast", "lunch", "dinner"] in week_planner.py:39. The blueprint says 'read the current WeekMeal prod model to find the slot vocabulary' but the vocabulary is actually in the service layer (week_planner.py:39 `SLOTS = ["breakfast", "lunch", "dinner"]`), not the model. The model only has a `slot: String` column with no enum constraint. The blueprint correctly says to hardcode or pass at init — just confirm the source is week_planner.py:39 (3 values: breakfast, lunch, dinner), not a model enum. — *FIX:* No code change needed; confirm the slot constant used in the engine matches `["breakfast", "lunch", "dinner"]` from week_planner.py:39. Add a comment in the engine citing that source.
- **[low]** Work item 7 sim check Step 3 (nil-target guard) asserts 'the pointer is preserved' after an EventGroceryItem arrives pointing at a non-existent GroceryItem target. But the blueprint's invariant list says the nil-target guard in unmerge 'skips subtraction, still clears pointers'. The sim check says 'pointer is preserved' (the dangling pointer is kept as-is). These are two different code paths: (a) unmerge where the target doesn't exist (pointers ARE cleared), vs (b) a freshly fetched EventGroceryItem whose target hasn't arrived yet (pointer preserved until target arrives). The sim check is testing (b) — correct. But the test assertion ('the pointer is preserved') must be tightly scoped: it should assert the pointer value is unchanged (the GroceryItem recordName string is still there), NOT that the target record exists. If the sim drains until convergence, the target may arrive before the assertion runs, making the 'nil-target guard' test vacuously pass. — *FIX:* In the sim check Step 3, assert the pointer IMMEDIATELY after the EventGroceryItem arrives (before any subsequent fetches), not after full convergence. Use a single fetchChanges() without a drain loop for this specific step.

### Review — sim-verifiable-proof (approved=False)

- **[critical]** EventGroceryItem.totalQuantity semantic mismatch — blueprint confuses event-row aggregate (the field's meaning) with GroceryItem.eventQuantity (cross-aggregate accumulation). Prod ORM (event.py:232) has total_quantity as the event row's OWN aggregated contribution from event meals, NOT a merge of multiple events. The blueprint's description ('event row's own aggregate contribution; distinct from GroceryItem.eventQuantity') is correct, but Work item 4 doesn't enforce a test that distinguishes the two in the codec. A codec round-trip test must verify: (1) an EventGroceryItem with totalQuantity=3 encodes/decodes to totalQuantity=3 (NOT 0 or nil), (2) a separate EventGroceryItem.eventQuantity field (if added later) stays independent. — *FIX:* In Work item 2 (EventGroceryCodecs.swift), add a comment to EventGroceryItemCodec.encode() explaining totalQuantity vs eventQuantity semantics (totalQuantity=this row's contribution, eventQuantity=cross-aggregate from GroceryItem unmerge tracking). In the codec round-trip test (Work item 2 verify), include TWO EventGroceryItem fields: totalQuantity=3.5 and a hypothetical eventQuantity field; verify both round-trip independently. If eventQuantity is NOT currently on EventGroceryItem, add a @INVARIANT comment that eventQuantity is owned by the GroceryItem.merge/unmerge pair, not EventGroceryItem.
- **[critical]** Event.eventDate — the blueprint prescribes nil=no-date and claims 'non-nil MUST be ISO-8601 YYYY-MM-DD', but does NOT verify the codec correctly enforces this at BOTH encode AND decode boundaries. The existing Event model has eventDate:String (work item 1 changes to String?). If the codec reads a TIMESTAMP from CloudKit as a Date, then attempts to assign it to String?, Swift will crash at decode time if not explicit Date→String conversion. The blueprint defers the actual codec to work item 2 but does NOT pre-test the Date↔String round-trip for correctness. — *FIX:* Add to Work item 2 verify (codec round-trip test): (1) EventCodec with eventDate='2026-07-04' (non-nil) encodes to TIMESTAMP, then decodes back to '2026-07-04' (string, not Date object). (2) EventCodec with eventDate=nil encodes the field as absent (nil in Swift = omitted key in CKRecord), then decodes back to nil. (3) Verify the ISO8601DateFormatter uses .withFullDate (YYYY-MM-DD only, not time component), and test that '2026-07-04T15:30:00Z' is REJECTED or stripped to date-only (prevent lexical ordering breaks). Add a test named 'eventDateCodecEnforcesFull Date' that would catch this pre-deployment.
- **[critical]** Post-batch repair pass wiring (Work item 5) is under-specified for week-scoped sibling recovery. The blueprint says 'collect affectedWeekNames by scanning modifications for GroceryItem.weekRef + EventGroceryItem.mergedIntoWeekID', but GroceryItem.weekRef does NOT EXIST in the current Models.swift — only GroceryItem has a weekID embedded nowhere. The spec §C says 'GroceryItem CASCADE parent = Week', so the codec MUST set a parent CKReference weekRef. But the blueprint does NOT confirm the weekRef field is added to Models.swift in Work item 1. This is a load-bearing gap: the repair pass cannot work without a way to reverse-lookup 'which week does this GroceryItem belong to'. — *FIX:* Work item 1 (Models.swift expansion): ADD a weekID:String field to GroceryItem (NOT a CKReference, just the string ID for in-memory scanning during repair). Document 'reverse-parent for post-batch repair sibling recovery'. Work item 4 (CKDSL): encode GroceryItem.weekRef as a CASCADE parent CKReference pointing to Week. Work item 5 (repair pass): scan by GroceryItem.weekID (from the decoded value type) not a CKReference traversal. Update the verify command to confirm GroceryCodec.encode sets the weekRef CKReference when the item is stored.
- **[critical]** Week.weekEnd is added in Work item 1 but the CKDSL block (Work item 4) assigns weekEnd STRING with NO index. Prod models (week.py:26) show week_end is a DATE, NOT string. The blueprint prescribes String (to parallel weekStart), but the CKDSL at Work item 4 says 'weekEnd STRING' with no TIMESTAMP conversion. This is IRREVERSIBLE: once deployed as STRING, it cannot be re-typed to TIMESTAMP for future queries. The fix is to either (A) keep it STRING and ensure codec does NO Date conversion (contradicts weekStart semantics), or (B) re-type to TIMESTAMP in the CKDSL to match week_start. Current code appears inconsistent. — *FIX:* Decide: is Week.weekEnd meant to be date-comparable with weekStart (both TIMESTAMP SORTABLE for range queries) or a free-form string? Prod uses Date for both. If keeping it STRING in the Swift type, update the CKDSL to TIMESTAMP SORTABLE to match weekStart (the codec will pass the Date through). If keeping STRING, document in a comment why range-query sortability is deferred. The test must verify Week.weekStart and Week.weekEnd are both the same type on the CloudKit side (both TIMESTAMP or both STRING), never mixed.
- **[high]** simVerify (Work item 7) does NOT prove the two-engine race for the post-batch repair pass. The spec says 'post-batch dedupe convergence: engineA creates two GroceryItems with identical (normalizedName, unit) on a test Week; engineB fetches; assert engineB's store has exactly one live and one tombstoned'. But the blueprint does NOT describe HOW the repair pass runs on engineB. Does engineB's fetch handler trigger the repair pass automatically? Or does engineB need a separate call to manually run repair? The test is ambiguous about the race — if the repair pass is deterministic-after-fetch (automatic), the test proves convergence. If it's manual-on-demand, the test proves nothing about a real two-device race where both devices are editing concurrently. — *FIX:* Clarify in Work item 5 (HouseholdSyncEngine changes) WHEN the repair pass runs: (a) AUTOMATIC on every .fetchedRecordZoneChanges (after the per-modification loop, before the handler returns), OR (b) MANUAL via an explicit `engine.runRepairPass()` call the app must invoke. Recommend (a) because two devices racing writes need automatic convergence, not a user-action gate. Update simVerify in Work item 7 to match: if (a), engineB's fetch automatically triggers repair and the test assertion is justified; if (b), the test must explicitly call engineB's repair method after fetching.
- **[high]** ConflictRepair.dedupeGrocery input filtering (line 32: 'let live = items.filter { !$0.isUserRemoved }') is correct but the post-batch repair pass in Work item 5 does NOT specify whether the caller pre-filters tombstones before calling dedupeGrocery. The spec for the repair pass says 'decode all GroceryItems from the store where weekRef points to weekName'. If any of those items are tombstones (isUserRemoved=true), the dedupe logic will silently skip them (line 32 filter). This is correct idempotency, but if a device creates a second loser-tombstone BEFORE the keeper is re-keyed by the first dedupe, the second run may not repoint links correctly. The test in Work item 5 verify (ConflictRepairTests.swift) checks idempotency but does NOT test the combined scenario: dedupeGrocery + tombstone-in-input + repoint links. — *FIX:* Add to ConflictRepairTests (Work item 5): a test named 'dedupeGroceryIdempotencyWithTombstoneInInput' that: (1) creates two live items + one loser-tombstone with a link pointing to it, (2) runs dedupe, (3) verifies the link is repointed to the keeper, (4) runs dedupe AGAIN, (5) verifies the second run produces zero new tombstones (the first-run tombstone is filtered, no double-tombstone). This proves the combined race: a device fetches mid-dedupe and sees mixed live+tombstone+repointed state, then re-runs repair without corrupting.
- **[high]** EventMergeEngine._matchKeys() (Work item 6) uses a DUAL-KEY fallback (baseKey=[baseIngredientID+unit or normalizedName+unit], nameKey=[normalizedName+unit]) per prod event_grocery.py:285-306. But the blueprint does NOT specify how the dual-key index is built in mergeEventIntoWeek. Prod code (event_grocery.py:328-335) builds a two-level dict: outer key = baseKey, inner-per-baseKey-list, then for each key, also check nameKey. The blueprint's pseudocode vaguely says 'Build the week index keyed by BOTH matchKey results' but does NOT prescribe the data structure (is it a [String:[GroceryItem]] per-baseKey dict? or a flat [(baseKey,nameKey)→item] dict?). This affects match-success probability — a loose implementation risks cache misses. — *FIX:* In Work item 6 (EventMergeEngine.swift), before writing mergeEventIntoWeek, add detailed pseudocode comments for the dual-key index construction: 'for each week grocery item, compute baseKey and nameKey (see _matchKeys). Build: weekIndexByBase[baseKey]=[...items], weekIndexByName[nameKey]=[...items]. For each event row: first try baseKey (higher specificity), fallback to nameKey. Exact code mirrors event_grocery.py:328-335.' Add a unit test `dualKeyFallbackPrefersBaseKey` that verifies an event row with baseIngredientID matches a week item with the same baseID even if normalizedName differs.
- **[high]** Week collapse (Work item 5, ConflictRepair.collapseDuplicateWeeks) has a critical race: the repair pass re-parents children (GroceryItem.weekRef = keeper recordID) but does NOT verify the keeper record exists in the store first. If engineB fetches a GroceryItem whose week field points to a loser-Week recordID before the keeper-Week arrives, the repair pass runs, updates the GroceryItem's weekRef to a keeper recordID that may NOT YET exist in the local store. This is a transient dangling ref — CloudKit's non-transactional per-record delivery means a GroceryItem can arrive before its parent. The nil-target guard in unmerge handles this for EventGroceryItem (Work item 6 invariant), but collapseDuplicateWeeks does NOT document or guard against dangling-parent in the re-parenting step. — *FIX:* In Work item 5 (HouseholdSyncEngine repair pass): after collapseDuplicateWeeks returns keeper+losers, do NOT immediately re-parent children. Instead, ENQUEUE the child-rewrite pending changes, but mark them deferred until the next sync cycle (or verify the keeper exists in store first). Alternatively, document in the repair-pass seam and the nullingDangling function: 'Week collapse re-parenting creates transient parent-dangles if the keeper arrives after the child. This is safe: the next repair pass or a manual null-check (ConflictRepair.nullingDangling) resolves it. Do NOT crash if weekRef points to a non-existent Week recordID.' Add a test in ConflictRepairTests: 'collapseDuplicateWeeksReparentingTolersateDanglingParent' — verify that repointed items with a non-existent parent recordID are stored without crash, and a later nullingDangling call cleans them up.
- **[high]** FieldMergeResolver.merge(EventGroceryItem) uses preferLive() to pick mergedIntoGroceryItemID and mergedIntoWeekID (lines 58-61), but the blueprint does NOT verify this behavior survives the expanded EventGroceryItem value type (Work item 1). The current 5-field EventGroceryItem is merged via the resolver (ResolverTests.swift:51-60). After Work item 1 expands it to 16 fields, the resolver MUST still compile and pass its existing test WITHOUT modification (since the resolver is pure and field-agnostic). However, the expanded fields (ingredientName, totalQuantity, etc.) introduce a subtle risk: if the resolver were accidentally re-written to use blanket LWW for those new fields (instead of preserving the existing per-field stickiness), the test would miss it because it only checks mergedIntoGroceryItemID/eventQuantity. The test does NOT check that ingredientName (a new sticky field per work item 6) is preserved correctly. — *FIX:* In Work item 2 (EventSyncMerger test in GroceryMergeSeamTests.swift), add a test 'eventGrocerySyncMergerPreservesNewStickyFields' that verifies: (1) engineA creates an EventGroceryItem with ingredientName='chicken' + all 16 new fields set, (2) engineB's remote comes back with ingredientName='pork' (the server carries a stale name from before the sync), (3) merge chooses the later-modified writer's name (LWW for non-sticky fields like ingredientName), but (4) a concurrent nil of mergedIntoGroceryItemID loses to a concurrent merge pointer (preferLive semantic). This proves the resolver handles the expanded type correctly.
- **[high]** CKDSL schema deployment (Work item 4) prescribes adding modifiedAtClock INT64 to the existing Event block, but the blueprint does NOT verify this is an ADDITIVE change to the already-deployed event.py schema. The current live event in phase0-schema.ckdb (lines 187-201) has no modifiedAtClock field. Adding it is safe (additive), but the validate-schema command user runs must RE-IMPORT the entire phase0-schema.ckdb file (cumulative by design). If the user only validates the new appended EventGroceryItem block in isolation, the validator will NOT see the modifiedAtClock addition to Event and may miss a critical field. The blueprint should prescribe exact xcodegen validation syntax. — *FIX:* In Work item 4 (CKDSL changes), specify the user's validate command as: 'xcrun cktool validate-schema --team-id <TEAM_ID> --container-id iCloud.app.simmersmith.cloud --environment development --file .docs/ai/phases/phase0-schema.ckdb' (validate the FULL file, not a subset). Add a comment in phase0-schema.ckdb above the Event block: 'Phase 4-remainder: additive modifiedAtClock INT64 to Event; appended EventGroceryItem/EventCodec blocks below.' This ensures the user understands the validation scope.
- **[medium]** EventMergeEngine._matchKeys coarser-than-MergeKey is documented correctly (Work item 6), but the blueprint does NOT prescribe a test that PROVES the coarser key is INTENTIONAL and not a regression. If a future implementer accidentally refactors _matchKeys to call the GroceryItem.mergeKey property, the test would pass (both match identically for the test case) but the event→week merge would silently regress (losing variation-sensitivity that week grocery merge requires). The test must affirmatively assert coarseness. — *FIX:* Add to EventMergeEngineTests: 'matchKeysAreCoarserThanMergeKey' — create an event row + week grocery with the SAME normalizedName+unit but DIFFERENT ingredientVariationID (e.g. 'chicken breast' locked vs unlocked variations). Verify the event row DOES match the week item (via _matchKeys), proving coarseness is real. Verify separately that GroceryItem.mergeKey would DIFFER between the two (if it were called). This proves intentional design, not accidental coarseness.
- **[medium]** The blueprint does NOT specify what happens to EventGroceryItem rows whose mergedIntoWeekID points to a week that was DELETED (not just missing transiently, but hard-deleted on a peer device). The nil-target guard in unmerge (Work item 6 invariant) handles a missing GroceryItem target, but NOT a missing Week target. If a Week is deleted AND a peer device has a dangling EventGroceryItem.mergedIntoWeekID pointing to it, the repair pass does NOT run (the week is gone), and the dangling link persists. The blueprint says nullingDangling cleans dangling refs, but does NOT call it for EventGroceryItem cross-week refs. — *FIX:* In Work item 5 (repair pass), after running dedupeGrocery + slot repair for each week, ALSO run: 'let allWeekIDs = store.allRecords().filter{ $0.recordType=="Week" }.map{ $0.recordName }. Call ConflictRepair.nullingDangling for each EventGroceryItem.mergedIntoWeekID against allWeekIDs; null any dangling refs.' Add a test in ConflictRepairTests: 'nullingDanglingWeekRefsOnEventGroceryItem' — create an EventGroceryItem with mergedIntoWeekID pointing to a non-existent Week, verify nullingDangling clears it.
- **[medium]** The blueprint prescribes Week.weekEnd field but does NOT verify the production date-range semantics. Prod models (week.py:26) show week_end is the calendar Sunday of the week_start Monday. Queries use `weekEnd >= someDate` to check if a week covers a date. If the codec and Value type use STRING for weekEnd, lexical comparison for date ranges works ONLY if BOTH are YYYY-MM-DD zero-padded. The blueprint documents this constraint for Event.eventDate but DOES NOT repeat it for Week.weekEnd, risking subtle bugs if a future date normalization step drops the padding. — *FIX:* Add to Week in Models.swift (Work item 1): comment '`weekEnd` is ISO-8601 YYYY-MM-DD (zero-padded) for lexical range-query compatibility with weekStart. See Week codec (HouseholdRecords target) for Date↔String conversion.' In the WeekCodec (part of the Phase 2b work, not this phase, but noted for clarity): explicitly use ISO8601DateFormatter.withFullDate for both weekStart and weekEnd. Add a test: 'weekRangeQuerySemantics' — verify '2026-06-09' < '2026-06-16' lexically, and that week_start=2026-06-09 + week_end=2026-06-15 is correctly range-checked.
- **[medium]** The post-batch repair pass runs after every fetchedRecordZoneChanges event (per Work item 5 spec), but the blueprint does NOT quantify the performance cost. At household scale (hundreds of grocery items), collecting affectedWeekNames + decoding-all-for-each-week + running dedupeGrocery + slot-repair + sort-reconcile is O(N*W) where N=total records, W=affected weeks. For 100 items + 10 affected weeks, that's 1000 decodes per fetch batch. With frequent syncs, this becomes a bottleneck. The blueprint notes this as a known O(N) scan but defers 'secondary index optimization' to a future phase. This is acceptable but must not be surprising at scale. — *FIX:* Add to Work item 5 (repair pass specification): 'KNOWN LIMITATION: The post-batch repair pass is O(N) — it scans all records to find affected-week candidates. At household scale (100–1000 items), this is acceptable. Future optimization (Phase X): add a secondary week-index in HouseholdLocalStore keyed by weekID to avoid full scans. For now, repair is automatic and correctness takes priority over latency. Monitor Fly logs for repair-pass duration if users report freezes.'
- **[medium]** The simVerify (Work item 7) mentions 'Week collapse: two engines simultaneously create a Week record with weekStart='2026-07-07' (same weekStart, different recordNames via PKs)' but does NOT verify the RECORDNAME derivation. Per spec §A, Week recordName is derived from `week:<householdID>:<weekStart>` (DET junction key, irreversible). If two engines on the SAME household create a week with the same weekStart, they MUST derive the SAME recordName — so there IS no collision, only one record is created. The blueprint seems to misunderstand the recordName policy: a true Week collision would be two DIFFERENT households' weeks with the same weekStart, which is impossible (the DET key includes householdID). Clarify the test intent. — *FIX:* In Work item 7 simVerify, clarify: 'Week recordName is derived from (householdID, weekStart) via DET junction key; two engines on the SAME household creating weekStart='2026-07-07' derive the SAME recordName and sync normally (LWW or merge as needed, not a collision). A true collision is prevented by the DET key including householdID. The test should instead verify: one week created on engineA, fetched on engineB, then both devices try to re-write it concurrently (e.g., engineA updates status, engineB updates notes) → the later writer wins (LWW) or if sticky fields exist, they merge.' If the intent was to test week deduplication across accounts (separate households), note that's out-of-scope for a single-device test (needs two iCloud accounts, a manual residual).
- **[low]** Work item 6 (EventMergeEngine) prescribes porting event_grocery.py:285-525 verbatim but does NOT check whether those functions have any caller-side dependencies (e.g., prod code that passes a `now` timestamp for time-based decisions). The blueprint lists function signatures but does NOT list parameters. For example, _resolve_target_week takes `event: Event, weeks: [Week]` per the blueprint but prod's _resolve_target_week (event_grocery.py:396) takes `(ev_row: EventGroceryItem, linking_week: Week?)` — the parameters are DIFFERENT. This is a name/signature mismatch that will break on porting. — *FIX:* Before writing EventMergeEngine, read prod event_grocery.py:396-525 LINEBYLINE and extract the ACTUAL function signatures (parameter names + types + defaults). Compare against the blueprint's prescribed signatures. If they differ, update the blueprint BEFORE implementation. Example: confirm _resolve_target_week's actual prod signature, its dependencies on global state (e.g., prod's `now` for date comparisons), and whether the Swift port can use `Date.now` or must accept a `now` parameter.
- **[low]** Work item 1 value-type expansions (EventGroceryItem, Week, Event) prescribe defaults for all new fields, but the blueprint does NOT verify that existing test call sites (ResolverTests.swift:51-60, line 77) still compile WITHOUT modification. The note claims 'changing a field to Optional means the literal non-nil value still works' — true for Event.eventDate:String? accepting "2026-07-04" — but DOES NOT check whether ALL downstream code reading eventDate is prepared for nil. The resolver (line 76-83) does NOT read eventDate, so the test passes, but a future merge or test that DOES read eventDate will silently get nil and may crash. — *FIX:* When implementing Work item 1, run 'swift test --package-path SimmerSmithCloudKit' AFTER the Models.swift edits and BEFORE committing. Verify all 52 existing tests pass. If any test reads a newly-optional field (eventDate, weekEnd) without guarding, add the guard. Document in the commit message: '52/52 tests green; optional-field additions are backward-compatible due to default nil values.'

## Phase 5 — Event↔week cross-aggregate merge (port merge_event_into_week/unmerge verbatim) + value-type expansion + on-sim convergence

### Blueprint (Sonnet)

**goal:**
Port merge_event_into_week / unmerge_event_from_week / apply_auto_merge_policy / _resolve_target_week / _match_keys verbatim from app/services/event_grocery.py onto the existing GroceryMerge + HouseholdSync module split. Expand thin value types (EventGroceryItem, Week) to carry the fields those functions read. Wire the new EventGroceryItem and Event codecs to the existing merger seam. Add the post-batch repair pass hook to the engine's fetchedRecordZoneChanges handler. Prove two-engine convergence on the simulator for the 3→6→9 double-count guard and the manuallyMerged pin.

**approach:**
Three concentric layers mirror the established GroceryMerge pattern: (1) pure headless logic in GroceryMerge (value-type expansion + EventMergeEngine), (2) thin CKRecord codecs in HouseholdSync (EventGroceryItemCodec + EventCodec, following GroceryCodec exactly), (3) engine wiring (new RecordMerger impls + post-batch repair hook). All schema decisions are authoring additions to phase0-schema.ckdb; a human validates and deploys via cktool before the sim check.

LAYER 0 — Value-type expansion (GroceryMerge/Models.swift, additive only):

EventGroceryItem needs 11 new fields to enable the verbatim port. Read Models.swift:95-107 — the current struct has {recordName, mergedIntoGroceryItemID, mergedIntoWeekID, eventQuantity, modifiedAt}. Add, all defaulted: totalQuantity: Double?, baseIngredientID: String?, ingredientVariationID: String?, ingredientName: String = "", normalizedName: String = "", unit: String = "", quantityText: String = "", category: String = "", sourceMeals: String = "", notes: String = "", reviewFlag: String = "", resolutionStatus: String = "". NOTE the naming decision that must be respected: Swift eventQuantity (Models.swift:99) is the field FieldMergeResolver.merge(EventGroceryItem) resolves via mergeEventQuantity — that stays as-is for the resolver. totalQuantity (new) is the event row's OWN contribution that merge_event_into_week reads at event_grocery.py:342 and unmerge reads at event_grocery.py:493. These are two distinct semantic slots. The EventGroceryItemCodec must map totalQuantity ↔ CKRecord["totalQuantity"] (match the prod column name). eventQuantity ↔ CKRecord["eventQuantity"] stays separate (the resolver's field for its own sticky semantics).

Week needs one new field: weekEnd: String = "". Required for the _resolve_target_week range query and the re-date branch of apply_auto_merge_policy. Both weekStart and weekEnd must be ISO-8601 YYYY-MM-DD for string lexical comparison to equal date comparison — the codec enforces this on encode.

Event.eventDate is currently String (non-optional). The port uses eventDate.isEmpty as the None equivalent, which is consistent with current call sites. Leave it non-optional (avoids breaking FieldMergeResolver.merge(Event) and existing init sites). Document the empty-string sentinel convention in a code comment at the field declaration.

GroceryItem: add storeLabel: String = "" as a round-trip safety field. Not phase-5-critical but prevents codec data loss. No merge logic needed.

Do not change any existing field names, types, or initializer parameter labels — only add new defaulted parameters. This preserves FieldMergeResolver, ConflictRepair, and all test files.

LAYER 1 — Pure orchestration (new file: GroceryMerge/EventMergeEngine.swift):

This file is CloudKit-free, headless-unit-testable, mirrors ConflictRepair's pure-enum style. It ports five functions from event_grocery.py verbatim.

EventMergeResult struct — the change-set returned by all three public entry points: changedGroceryItems: [GroceryItem] (both updated existing + newly created), changedEventLinks: [EventGroceryItem] (pointer updates), updatedEvent: Event, deletedGroceryItemNames: [String] (self-deleted event-only rows from unmerge).

_matchKeys helper — verbatim port of event_grocery.py:285-306. Returns (baseKey: String, nameKey: String). baseKey = "(baseIngredientID)\u{1}(unit)" when baseIngredientID is non-empty, else "\u{1}(unit)\u{1}(normalizedName)". nameKey = always "\u{1}(unit)\u{1}(normalizedName)". NOTE this is coarser than MergeKey (no quantityText, no locked-variation). Put a code comment at the top of the function: "Port of _match_keys (event_grocery.py:285-306). NOT the same as Models.MergeKey — do not conflate. MergeKey is for CKSyncEngine field-level conflict resolution; _matchKeys is for event-row→week-row matching during merge." The unit participates in BOTH keys, so "2 lb chicken" never merges with "1 oz chicken".

mergeEventIntoWeek(event: Event, eventLinks: [EventGroceryItem], weekGroceries: [GroceryItem]) -> EventMergeResult — port of event_grocery.py:309-393. Build a week index keyed by both _matchKeys outputs (same row may appear under two keys). For each eventLink: (1) idempotency skip if mergedIntoWeekID == week.recordName AND mergedIntoGroceryItemID is non-nil (event_grocery.py:340-341); (2) if eventLink.totalQuantity is nil, set mergedIntoWeekID only, no numeric add, count as text-only (event_grocery.py:342-348); (3) if a match exists, add eventLink.totalQuantity to match.eventQuantity using roundedSum (same helper as ConflictRepair, round to 2dp), set both merged_into pointers (event_grocery.py:351-361); (4) no match: create a new GroceryItem with totalQuantity=nil, eventQuantity=eventLink.totalQuantity, sourceMeals="event:\(event.name)" (T4 name-agnostic prefix), week-identity fields from eventLink (event_grocery.py:366-390). After the loop, return updatedEvent with linkedWeekID = week.recordName.

unmergeEventFromWeek(event: Event, eventLinks: [EventGroceryItem], weekGroceries: [GroceryItem], keepLink: Bool) -> EventMergeResult — port of event_grocery.py:475-525. For each eventLink where mergedIntoWeekID == week.recordName: look up mergedIntoGroceryItemID in weekGroceries; NIL-TARGET GUARD — if target is nil or eventLink.totalQuantity is nil, still clear the ev-row pointers but skip the subtraction (event_grocery.py:493 "if target is not None and ev_row.total_quantity is not None"); subtract totalQuantity from target.eventQuantity; set to nil when <= 0.0001 (port the 0.0001 threshold exactly — event_grocery.py:496); strip legacy "+event: X" notes (event_grocery.py:499-503); self-delete decision: event-only check is target.sourceMeals.hasPrefix("event:") (NOT equality, event_grocery.py:509); no_remaining_qty = target.totalQuantity ?? 0 <= 0 AND target.eventQuantity ?? 0 <= 0; no_user_investment = all overrides nil AND !target.check.isChecked AND !target.isUserAdded; if all three: add target.recordName to deletedGroceryItemNames; always clear ev-row pointers. If !keepLink and event.linkedWeekID == week.recordName: clear event.linkedWeekID (event_grocery.py:523-524).

resolveTargetWeek(event: Event, weeks: [Week]) -> Week? — port of event_grocery.py:396-423. If event.linkedWeekID is non-nil and non-empty, return the matching week (explicit link wins). If event.eventDate.isEmpty, return nil. Else find the latest-starting week where weekStart <= eventDate AND weekEnd >= eventDate (string lexical comparison, safe for YYYY-MM-DD). Tiebreak = largest weekStart (DESC) per M71 (event_grocery.py:413: "ORDER BY week_start DESC LIMIT 1").

applyAutoMergePolicy(event: Event, eventLinks: [EventGroceryItem], allGroceries: [GroceryItem], weeks: [Week]) -> EventMergeResult — port of event_grocery.py:426-472. Branch 1: if event.manuallyMerged AND event.linkedWeekID is non-nil (event_grocery.py:449-454) — find linked week in weeks, call mergeEventIntoWeek, return, NEVER auto-unmerge. Branch 2: if event.autoMergeGrocery (event_grocery.py:456-465) — if linkedWeekID set AND eventDate non-empty AND eventDate moved off linked week's [weekStart, weekEnd] range: call unmergeEventFromWeek(keepLink: false), then resolveTargetWeek, then mergeEventIntoWeek into the new target. If no prior link: just resolveTargetWeek and merge. Branch 3: neither flag (event_grocery.py:468-472) — if linkedWeekID set, unmerge(keepLink: false).

LAYER 2 — CKRecord codecs (HouseholdSync, follow GroceryCodec exactly):

EventGroceryItemCodec.swift — mirrors GroceryCodec.swift pattern: recordType = "EventGroceryItem", makeRecord/encode(into:)/decode. Field mapping (all field names match prod column names per the schema): totalQuantity → Double?, mergedIntoWeekID → String?, mergedIntoGroceryItemID → String?, eventQuantity → Double?, modifiedAtClock → Int (INT64, same as GroceryCodec's modifiedAtClock), ingredientName/normalizedName/unit/quantityText/category/sourceMeals/notes/reviewFlag/resolutionStatus → String, baseIngredientID/ingredientVariationID → String? (setOpt). eventRef parent → CKRecord.Reference with action .deleteSelf (CASCADE parent per phase0-schema.md §C: Event→EventGroceryItem is CASCADE). Use setOpt helper from GroceryCodec for all optional fields.

EventCodec.swift — recordType = "Event". Fields: name/eventDate/linkedWeekID → String, manuallyMerged/autoMergeGrocery → INT64 Bool (0/1 pattern from GroceryCodec). modifiedAtClock → Int (INT64). IMPORTANT: the existing Event CKDSL block (phase0-schema.ckdb:187-201, authored in Phase 2b via HouseholdRecordType) uses eventDate as TIMESTAMP SORTABLE. The EventCodec must encode eventDate as CKRecordValue via a Date parsed from YYYY-MM-DD (if non-empty), or nil when empty-string sentinel. Decode back to YYYY-MM-DD string from CKRecord Date. This bridges the CKDSL TIMESTAMP type with the Swift String value type correctly. modifiedAtClock is NOT in the existing 2b CKDSL block — it must be added to phase0-schema.ckdb as an additive field (INT64).

WeekCodec.swift — recordType = "Week". Follow GroceryCodec. Fields: weekStart/weekEnd → String (encode as CKRecord Date from YYYY-MM-DD, decode back to string). weekEnd is a new additive field in the CKDSL. modifiedAtClock → INT64.

LAYER 3 — RecordMerger implementations (HouseholdSync/RecordMerger.swift, extend existing):

EventGrocerySyncMerger — handles "EventGroceryItem". resolve() decodes both records via EventGroceryItemCodec, calls FieldMergeResolver.merge (the existing merge(_ a: EventGroceryItem, _ b: EventGroceryItem) at FieldMergeResolver.swift:51), encodes onto remote copy, needsResave = merged != remoteValue. NOTE: FieldMergeResolver.merge(EventGroceryItem) only merges the pointer + eventQuantity fields (the sticky pointer semantics). The totalQuantity and ingredient identity fields are LWW (no special sticky semantics needed — they are set once during aggregation and are not concurrently written by two paths). The merge function takes the LWW base (higher modifiedAt) and applies the preferLive pointer semantics. Since the existing merge function references eventQuantity for the mergeEventQuantity logic, and we are not renaming that field, this wires correctly.

EventSyncMerger — handles "Event". resolve() decodes via EventCodec, calls FieldMergeResolver.merge (the existing merge(_ a: Event, _ b: Event) at FieldMergeResolver.swift:76), encodes onto remote copy. The manuallyMerged sticky OR semantics are already in the resolver.

Both can be installed as a single CompositeSyncMerger that dispatches by recordType, or as two separate merger installations. Mirror GrocerySyncMerger's pattern (struct conforming to RecordMerger). The engine's merger property is currently a single RecordMerger? — to support multiple types, either: (a) make merger an array [RecordMerger] and iterate handles(), or (b) create a DispatchingMerger wrapper that holds [RecordMerger] and delegates. Option (b) is cleaner and non-breaking. Add DispatchingMerger to RecordMerger.swift.

LAYER 4 — Post-batch repair pass (HouseholdSyncEngine.swift, additive):

The hook point is inside case .fetchedRecordZoneChanges (line 148), after the per-record modification loop and deletion loop complete, before the handler returns. This is already the cleanest hook — confirmed by the ConflictRepair analysis.

During the modification loop, collect affectedWeekIDs: Set<String> by decoding any modified record whose recordType is "GroceryItem" or "EventGroceryItem" and reading its weekRef field (weekID for GroceryItem, mergedIntoWeekID for EventGroceryItem). After the deletion loop, for each weekID in affectedWeekIDs: call store.allRecords(), decode all records, filter by type+weekID, then call ConflictRepair.dedupeGrocery(items:eventLinks:). From GroceryDedupeResult: encode+save keepers (store.setRecord + syncEngine.state.add(.saveRecord)), encode+save tombstoned (same — NOT deleteRecord), encode+save repointedLinks (same). Also run collapseDuplicateWeeks, repairDuplicateSlots, and reconcileSortOrder for any weeks in affectedWeekIDs where WeekMeal records were also modified. The scan-and-decode approach (Option A from the analysis) is correct — O(N) over the household store is fine at household scale.

SCHEMA additions to phase0-schema.ckdb (IRREVERSIBLE — human deploys via cktool before sim check):

Add the following to phase0-schema.ckdb (cumulative, append after the GroceryItem block):

RECORD TYPE Week — fields: weekStart STRING QUERYABLE SORTABLE, weekEnd STRING, modifiedAtClock INT64, plus any status/notes/timestamp fields from week.py:18-35 as needed (status STRING, notes STRING, readyForAIAt TIMESTAMP, approvedAt TIMESTAMP). No parent reference (top-level). GRANT WRITE TO "_creator", GRANT READ,CREATE TO "_icloud".

RECORD TYPE WeekMeal — fields: weekRef REFERENCE (CASCADE parent), dayName STRING, slot STRING, sortOrder INT64, modifiedAtClock INT64. Defer mealDate, recipeName, and other prod fields unless ConflictRepair.repairDuplicateSlots requires them (it does not — slot/dayName/sortOrder/weekID are sufficient). GRANT same.

RECORD TYPE EventGroceryItem — fields: ingredientName STRING, normalizedName STRING QUERYABLE, totalQuantity DOUBLE, unit STRING, quantityText STRING, category STRING, sourceMeals STRING, notes STRING, reviewFlag STRING, resolutionStatus STRING, mergedIntoWeekID STRING, mergedIntoGroceryItemID STRING, modifiedAtClock INT64, event REFERENCE (CASCADE parent — Event→EventGroceryItem), baseIngredientID STRING, ingredientVariationID STRING. GRANT same. mergedIntoWeekID and mergedIntoGroceryItemID are plain STRING (not CKReference) per phase0-schema.md §C (SET-NULL soft pointers).

Update RECORD TYPE GroceryItem (already in schema at line 314) — add TWO additive fields: weekRef REFERENCE (CASCADE parent — Week→GroceryItem is CASCADE per week.py:188), storeLabel STRING. Also add ingredientName STRING as a pass-through field (prod NOT-NULL column absent from value type — prevents data loss on round-trip).

Update RECORD TYPE Event (already in schema at line 187) — add additive field: modifiedAtClock INT64. This is required by EventCodec and FieldMergeResolver.merge(Event).

These schema changes must be deployed to DEV before the sim check. The human runs: xcrun cktool validate-schema ... && xcrun cktool import-schema .... Do NOT promote to Production until the recordName policy and field types are confirmed stable.

**valueTypeChanges:**
- EventGroceryItem: +totalQuantity: Double?, +baseIngredientID: String?, +ingredientVariationID: String?, +ingredientName: String='', +normalizedName: String='', +unit: String='', +quantityText: String='', +category: String='', +sourceMeals: String='', +notes: String='', +reviewFlag: String='', +resolutionStatus: String=''
- Week: +weekEnd: String='' (ISO-8601 YYYY-MM-DD, enforced by WeekCodec on encode)
- Event.eventDate: no type change; add code comment documenting empty-string-as-None sentinel
- GroceryItem: +storeLabel: String='' (round-trip safety, LWW)

**invariants:**
- EventGroceryItem.eventQuantity (existing) and EventGroceryItem.totalQuantity (new) are DISTINCT fields with different semantics: eventQuantity is the resolver's sticky field (mergeEventQuantity: prefer non-nil, take max); totalQuantity is the event row's own aggregated contribution that EventMergeEngine reads when adding to week GroceryItem.eventQuantity. Do not conflate them. EventGroceryItemCodec maps totalQuantity <-> CKRecord['totalQuantity'] (prod column name) and eventQuantity <-> CKRecord['eventQuantity'] (separate).
- _matchKeys (EventMergeEngine) is coarser than MergeKey (Models.swift). Do not reuse or conflate them. _matchKeys has no quantityText, no locked-variation; unit always participates. A unit mismatch (lb vs oz) must never merge.
- Tombstone not delete: ConflictRepair.dedupeGrocery losers have isUserRemoved=true and must be encoded and enqueued as .saveRecord, never .deleteRecord. A hard delete lets a peer that hasn't seen the dedup re-create the row (no tombstone to suppress it).
- ISO-8601 YYYY-MM-DD is the load-bearing format for weekStart, weekEnd, eventDate. String lexical comparison is safe for date range queries only under this format. WeekCodec and EventCodec must enforce it on encode and validate on decode.
- unmerge nil-target guard: if mergedIntoGroceryItemID is non-nil but the target GroceryItem is absent from the provided set (cross-aggregate atomicity race: EventGroceryItem arrived before its target), skip the subtraction but still clear the ev-row pointers. Never crash on a missing target.
- 0.0001 threshold in unmerge: set target.eventQuantity to nil when the post-subtraction value <= 0.0001. Port exactly from event_grocery.py:496 — floating-point subtraction can produce small positive residuals that should resolve to nil.
- source_meals 'event:' prefix check (isEventOnly) is a PREFIX check (.hasPrefix('event:')), NOT equality with 'event:{name}'. A rename of the event between merge and unmerge must not strand a zombie row.
- manuallyMerged is sticky: applyAutoMergePolicy never clears it. Only an explicit unmerge call clears both the linkedWeekID and manuallyMerged. The FieldMergeResolver.merge(Event) already enforces OR-semantics on manuallyMerged — a concurrent re-unmerge on one device while a merge is pending on another must preserve the pin.
- regenerate (unmerge keep_link=true THEN rebuild EventGroceryItems THEN re-merge) must be called in that exact order. Without unmerge-before-wipe, a re-merge adds the new contribution on top of the stale one producing double-count.
- The post-batch repair pass must filter tombstoned rows from the dedupe input before calling ConflictRepair.dedupeGrocery. The dedupeGrocery function does this internally (ConflictRepair.swift:32), but passing the full store output including tombstones is safe because the filter is inside dedupeGrocery. Re-running over a store that includes prior tombstones is idempotent.
- mergedIntoWeekID and mergedIntoGroceryItemID on EventGroceryItem encode as plain String (not CKReference) in the CKDSL and in the codec. These are SET-NULL soft pointers per phase0-schema.md §C line 84. Using CKReference here would prevent the nullingDangling pattern from working and would fail validate-schema if the pointer format is wrong.

**headlessTests:**
- cd /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit && swift test (all 52 existing tests must remain green; EventMergeEngineTests adds the new cases)
- swift test --filter EventMergeEngineTests (fast path for the new suite alone)
- EventMergeEngineTests: 3->6->9 guard — call mergeEventIntoWeek, then unmergeEventFromWeek(keepLink:true) + rebuild EventGroceryItem (new totalQuantity=3) + mergeEventIntoWeek again; assert changedGroceryItems.first.eventQuantity == 3 (not 6, not 9)
- EventMergeEngineTests: unmerge-to-zero self-delete — event-only GroceryItem (sourceMeals hasPrefix 'event:', no overrides, isChecked=false); after unmerge assert deletedGroceryItemNames contains that recordName
- EventMergeEngineTests: unmerge preserves user-invested row — same setup but check.isChecked=true; assert deletedGroceryItemNames is empty
- EventMergeEngineTests: text-only row — EventGroceryItem with totalQuantity=nil; after mergeEventIntoWeek assert mergedIntoWeekID is set but no GroceryItem.eventQuantity changed
- EventMergeEngineTests: _matchKeys catalog-resolved match — ev_row has baseIngredientID='b1', unit='cup'; week row also has baseIngredientID='b1', unit='cup'; assert they match. ev_row with nil baseIngredientID, normalizedName='tomato', unit='lb' vs week row same; assert match. ev_row unit='lb' vs week row unit='oz'; assert NO match
- EventMergeEngineTests: manuallyMerged pin — event with manuallyMerged=true, linkedWeekID='W1'; applyAutoMergePolicy with eventDate moved off W1 range; assert result still targets W1 (not re-resolved to W2), linkedWeekID == 'W1'
- EventMergeEngineTests: re-date branch — event with autoMergeGrocery=true, linkedWeekID='W1', eventDate moved off W1's weekEnd; applyAutoMergePolicy with W2 available covering the new date; assert unmerge from W1 + merge into W2
- EventMergeEngineTests: M71 tiebreak — two weeks both covering event date; latest weekStart wins as target
- EventMergeEngineTests: nil-target guard — EventGroceryItem with mergedIntoGroceryItemID pointing to a recordName absent from weekGroceries; unmerge must not crash; pointers cleared, no subtraction attempted
- swift build --package-path /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit (confirms new codecs compile in HouseholdSync Swift 5 mode)

**risks:**
- IRREVERSIBLE CKDSL: Week, WeekMeal, EventGroceryItem are new record types; weekRef on GroceryItem, storeLabel/ingredientName on GroceryItem, and modifiedAtClock on Event are additive fields. Field names, types (especially DOUBLE vs INT64), and the weekRef parent REFERENCE action are one-way. Validate with cktool before import to DEV. Do NOT import to Production until the record-type shape is confirmed stable across a full two-device round-trip on DEV.
- weekRef REFERENCE on GroceryItem is a CASCADE parent edge (Week->GroceryItem is CASCADE per week.py:188). This means adding weekRef requires every GroceryItem save to carry the weekRef field. If any existing test or sim check creates a GroceryItem without weekRef, CloudKit will accept it (REFERENCE fields are optional in the schema) but the cascade sweep will miss it. The codec must always encode weekRef when the GroceryItem belongs to a week.
- EventCodec bridges eventDate String <-> CKDSL TIMESTAMP. The existing 2b CKDSL Event block (phase0-schema.ckdb:189) already declares eventDate as TIMESTAMP SORTABLE. If the codec fails to parse the YYYY-MM-DD string into a Date before encoding, CloudKit will reject the record (type mismatch). The codec must guard the parse and fall back gracefully for empty eventDate (encode as nil/absent).
- FieldMergeResolver.merge(EventGroceryItem) at FieldMergeResolver.swift:51 references eventQuantity (the sticky field) on the Swift value type. After the value-type expansion adds totalQuantity, confirm the resolver still compiles and the field references are unambiguous. The resolver does NOT touch totalQuantity or any of the new ingredient identity fields — those are LWW via the base winner selection.
- Two engines on one zone (the sim check pattern) is the §4.2-safe two-device model only when each engine has its own stateURL and automaticSync=false. If the sim check accidentally shares a stateURL between both engines, the change token is forked and the check becomes invalid. Mirror runHouseholdSyncCheck's temp-directory state file pattern exactly.
- The post-batch repair pass calls syncEngine.state.add() inside handleEvent. Per CKSyncEngine docs this is safe (it buffers). However, if the repair pass adds pending saves for tombstones that were JUST fetched as remote modifications, the engine may loop (fetch -> repair -> save -> fetch -> ...). Guard: only enqueue saves for records whose encoded value actually changed from the incoming remote version (compare before enqueue, same needsResave pattern as GrocerySyncMerger).

**filesToCreate:**
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Sources/GroceryMerge/EventMergeEngine.swift — Pure (CloudKit-free) port of merge_event_into_week / unmerge_event_from_week / apply_auto_merge_policy / _resolve_target_week / _match_keys from event_grocery.py. Operates on GroceryItem/EventGroceryItem/Event/Week value types, returns EventMergeResult change-set struct. Headless-unit-testable; mirrors ConflictRepair's pure-enum style. No CloudKit import.
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Sources/HouseholdSync/EventGroceryItemCodec.swift — CKRecord <-> EventGroceryItem codec following the GroceryCodec pattern exactly (recordType, makeRecord, encode(into:), decode). Encodes totalQuantity/mergedInto pointers/ingredient identity fields. eventRef parent as .deleteSelf CKReference. modifiedAtClock as INT64. mergedIntoWeekID and mergedIntoGroceryItemID as plain String (not CKReference). Guarded by #if canImport(CloudKit).
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Sources/HouseholdSync/EventCodec.swift — CKRecord <-> Event codec. Bridges eventDate String <-> CKDSL TIMESTAMP via YYYY-MM-DD ISO-8601 parsing (empty string encodes as nil/absent field). manuallyMerged/autoMergeGrocery as INT64. modifiedAtClock as INT64. linkedWeekID as plain String. Guarded by #if canImport(CloudKit).
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Sources/HouseholdSync/WeekCodec.swift — CKRecord <-> Week codec. weekStart and weekEnd encoded as CKRecord Date from YYYY-MM-DD, decoded back to YYYY-MM-DD string. modifiedAtClock as INT64. Enforces ISO-8601 format on encode (the load-bearing invariant for lexical range comparisons). Guarded by #if canImport(CloudKit).
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Tests/GroceryMergeTests/EventMergeEngineTests.swift — Swift Testing headless suite for EventMergeEngine. Tests: 3->6->9 double-count guard (merge/unmerge-keep-link/rebuild/re-merge converges to eventQuantity==3); unmerge-to-zero self-deletes event-only rows and respects user investment; text-only row (totalQuantity nil) sets pointer only with no numeric add; _matchKeys base vs name key fallback and unit-mismatch guard; manuallyMerged pin short-circuits auto-unmerge; re-date branch unmerges and re-resolves; M71 latest-starting week tiebreak. Mirrors ConflictRepairTests.swift style.

**filesToModify:**
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Sources/GroceryMerge/Models.swift — Additive only — no existing field or init label changes. (1) EventGroceryItem: add 11 defaulted fields: totalQuantity: Double? = nil, baseIngredientID: String? = nil, ingredientVariationID: String? = nil, ingredientName: String = '', normalizedName: String = '', unit: String = '', quantityText: String = '', category: String = '', sourceMeals: String = '', notes: String = '', reviewFlag: String = '', resolutionStatus: String = ''. Add a code comment: 'eventQuantity (existing) is the resolver's sticky field (FieldMergeResolver.merge applies mergeEventQuantity). totalQuantity (new) is the event row's own contribution, read by EventMergeEngine — maps to prod total_quantity column.' (2) Week: add weekEnd: String = ''. Add comment: 'ISO-8601 YYYY-MM-DD. Required for _resolve_target_week range comparison. Lexical order == date order only when format is enforced by WeekCodec on encode.' (3) Event.eventDate: add code comment: 'Non-optional String. Empty string = no date (maps to prod nullable event_date: Mapped[date | None]). applyAutoMergePolicy uses eventDate.isEmpty as the None branch.' (4) GroceryItem: add storeLabel: String = '' (round-trip safety, no merge semantics, LWW). Extend initializer to include all new params with defaults.
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Sources/HouseholdSync/RecordMerger.swift — Add DispatchingMerger struct that holds [RecordMerger] and conforms to RecordMerger: handles() returns true if any inner merger handles the type; resolve() delegates to the first handling merger. Add EventGrocerySyncMerger struct (handles 'EventGroceryItem', decodes via EventGroceryItemCodec, calls FieldMergeResolver.merge(_ a: EventGroceryItem, _ b: EventGroceryItem), encodes onto remote copy, needsResave = merged != remoteValue) and EventSyncMerger struct (handles 'Event', decodes via EventCodec, calls FieldMergeResolver.merge(_ a: Event, _ b: Event), encodes onto remote copy). GrocerySyncMerger stays unchanged.
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Sources/HouseholdSync/HouseholdSyncEngine.swift — Post-batch repair pass only — do NOT change the per-record merger dispatch (already correct at lines 149-174). Inside case .fetchedRecordZoneChanges, after the modifications loop and deletions loop complete: (1) Collect affectedWeekIDs: Set<String> by decoding each modified record during the loop — if recordType == GroceryCodec.recordType, read the weekRef field (a CKRecord.Reference or String); if recordType == EventGroceryItemCodec.recordType, read mergedIntoWeekID. (2) After the loops, for each weekID: call store.allRecords(), decode and filter by type+weekID, run ConflictRepair.dedupeGrocery(items:eventLinks:). From the result: encode keepers and tombstoned via GroceryCodec and call store.setRecord + syncEngine.state.add(.saveRecord) for both (never deleteRecord for tombstones). Encode repointedLinks via EventGroceryItemCodec and save+enqueue. (3) For any WeekMeal records in affectedWeekIDs: decode and run repairDuplicateSlots then reconcileSortOrder; save changed meals. (4) Run collapseDuplicateWeeks over all Week records if any Week modifications arrived. Keep the repair pass gated — only run when affectedWeekIDs is non-empty.
- /Users/tfinklea/git/simmersmith/SimmerSmithCloudKit/Sources/HouseholdSync/GroceryCodec.swift — Add storeLabel and ingredientName to encode(into:) and decode(_:) as plain String fields (pass-through, LWW). No change to any existing field encoding. These prevent round-trip data loss for prod columns absent from the value type.
- /Users/tfinklea/git/simmersmith/.docs/ai/phases/phase0-schema.ckdb — Append four new blocks and update two existing blocks. (1) NEW: RECORD TYPE Week (weekStart STRING QUERYABLE SORTABLE, weekEnd STRING, modifiedAtClock INT64, status STRING, notes STRING, readyForAIAt TIMESTAMP, approvedAt TIMESTAMP, GRANT WRITE TO '_creator', GRANT READ,CREATE TO '_icloud'). (2) NEW: RECORD TYPE WeekMeal (weekRef REFERENCE, dayName STRING, slot STRING, sortOrder INT64, modifiedAtClock INT64, GRANT same). (3) NEW: RECORD TYPE EventGroceryItem (ingredientName STRING, normalizedName STRING QUERYABLE, totalQuantity DOUBLE, unit STRING, quantityText STRING, category STRING, sourceMeals STRING, notes STRING, reviewFlag STRING, resolutionStatus STRING, mergedIntoWeekID STRING, mergedIntoGroceryItemID STRING, modifiedAtClock INT64, event REFERENCE, baseIngredientID STRING, ingredientVariationID STRING, GRANT same). (4) UPDATE RECORD TYPE GroceryItem (add additive fields: weekRef REFERENCE, storeLabel STRING, ingredientName STRING). (5) UPDATE RECORD TYPE Event (add additive field: modifiedAtClock INT64). IRREVERSIBLE — human validates with cktool before import to DEV. Do NOT add normalizedName QUERYABLE to EventGroceryItem unless it is in §B (it is not; only GroceryItem.normalizedName is in §B — do not add extra indexes).
- /Users/tfinklea/git/simmersmith/SimmerSmith/SimmerSmith/Features/Settings/CloudKitDebugView.swift — Add a 'Phase 5 — event↔week cross-aggregate merge' Button and runEventMergeCheck() async function inside the #if DEBUG block. Mirror runHouseholdSyncCheck exactly: two HouseholdSyncEngine instances, shared zone, temp state files, automaticSync=false. Configure both engines with a DispatchingMerger([GrocerySyncMerger(), EventGrocerySyncMerger(), EventSyncMerger()]). Drive sync() manually. Implement four checks (initial merge, 3->6->9 guard, unmerge self-delete, pin survives concurrent edit) with print per-step pass/fail trace. The poll-until-converged helper must tolerate a transiently-dangling mergedIntoGroceryItemID (keep polling, not fail-fast).

**simVerify:** Add a "Phase 5 — event↔week cross-aggregate merge" button to CloudKitDebugView.swift (in the existing #if DEBUG block, mirroring runHouseholdSyncCheck exactly: two HouseholdSyncEngines on one shared zone with automaticSync=false and distinct temp stateURLs, each engine configured with a DispatchingMerger holding GrocerySyncMerger + EventGrocerySyncMerger + EventSyncMerger). Drive sync() manually. Use the existing valueInB-style poll-until-converged helper.

Check 1 — Initial merge converges: engineA creates Week W (weekStart/weekEnd in YYYY-MM-DD), GroceryItem G on W (totalQuantity=3, eventQuantity=nil), Event E (eventDate inside W's range), EventGroceryItem EG (totalQuantity=3, pointing nowhere). Run EventMergeEngine.mergeEventIntoWeek. Save+sync. engineB fetches. Assert engineB sees G.eventQuantity==3 (NOT 6). This confirms the merge did not double-apply.

Check 2 — 3→6→9 guard (the headline invariant from spec §5 Verify): on engineA, simulate regenerate: call unmergeEventFromWeek(keepLink:true) (clears EG pointers, subtracts from G), rebuild EG (new EventGroceryItem with totalQuantity=3), call mergeEventIntoWeek again. Save+sync. engineB fetches. Assert engineB still sees G.eventQuantity==3 (not 6, not 9). A broken unmerge-before-rebuild produces 6; a broken re-merge produces 9.

Check 3 — Unmerge self-delete: event-only GroceryItem (sourceMeals="event:TestParty", no overrides, check.isChecked=false, isUserAdded=false). Unmerge → assert deletedGroceryItemNames contains G.recordName, and after engineB fetches, G is tombstoned (isUserRemoved=true) or absent. Repeat with check.isChecked=true set on G BEFORE unmerge → assert G is NOT deleted (user investment preserves it).

Check 4 — manuallyMerged pin survives concurrent edit: engineA sets E.manuallyMerged=true. Concurrently (before sync), engineB edits E.name (different field). Both sync. Assert both engines see E.manuallyMerged=true (sticky OR via FieldMergeResolver.merge(Event)).

The sim check asserts what headless tests structurally cannot: live two-engine delivery ordering (EG arriving before G = transient dangling pointer is expected; the poll loop must allow for it without asserting failure mid-poll). Print a per-step pass/fail trace following the existing CloudKitDebugView trace format.

### Review — merge-invariants (approved=False)

- **[blocker]** manuallyMerged pin survives concurrent edit — but the blueprint's applyAutoMergePolicy port has a silent unmerge path that CAN clear manuallyMerged indirectly. Branch 3 ('neither flag') calls unmergeEventFromWeek(keepLink:false), which clears event.linkedWeekID. Then Branch 1's guard 'if event.manuallyMerged AND event.linkedWeekID is non-nil' would silently skip the re-merge on the next call because linkedWeekID is now nil — the pin bit stays true but the link is gone, so the event is stranded. In the server (event_grocery.py:449-454) this can't happen because manually_merged can only co-exist with linked_week_id (the explicit merge endpoint sets both together). In the CloudKit multi-device path, a concurrent edit could deliver manuallyMerged=true from one device and linkedWeekID=nil from another in one CKSyncEngine batch. After the FieldMergeResolver.merge(Event), manuallyMerged is sticky-OR'd to true (correct), but linkedWeekID uses preferLive which prefers non-nil — correct if both arrive together, but not if they arrive in separate fetches with different modifiedAt clocks. The blueprint's sim Check 4 only covers a concurrent NAME edit, not a concurrent linkedWeekID edit, so this race is untested. — *FIX:* Add a sim Check 4b: engineA sets manuallyMerged=true AND linkedWeekID='W1'. engineB concurrently clears linkedWeekID (simulates an explicit unmerge arriving out of order). After convergence assert that the merged Event has manuallyMerged preserved (OR) AND that applyAutoMergePolicy on a subsequent call does NOT silently skip the merge due to nil linkedWeekID. Also: document in applyAutoMergePolicy that Branch 1's linkedWeekID guard is the defense against a pin-without-link stranded state, and add a headless test asserting applyAutoMergePolicy with manuallyMerged=true but linkedWeekID=nil returns a no-op result (no crash, no auto-resolve).
- **[blocker]** 3→6→9 double-count guard depends on unmerge-before-wipe ordering, but the blueprint's LAYER 1 EventMergeEngine.mergeEventIntoWeek idempotency skip is WRONG. The blueprint says: 'idempotency skip if mergedIntoWeekID == week.recordName AND mergedIntoGroceryItemID is non-nil'. But in the 3→6→9 regen scenario, regenerate calls unmerge(keepLink:true) FIRST, which CLEARS mergedIntoGroceryItemID (and mergedIntoWeekID). Then it wipes and rebuilds the EventGroceryItem with a NEW recordName (session.add creates a new row — see event_grocery.py:258-276). The new EG has mergedIntoWeekID=nil and mergedIntoGroceryItemID=nil, so the idempotency check correctly passes through. But the blueprint's Layer 1 code says the rebuild creates a 'new EventGroceryItem with totalQuantity=nil' for the no-match case — that is NOT the regen flow, that is a merge-created week GroceryItem. The regen flow creates new EventGroceryItems (the EG rows themselves) then calls merge. The blueprint conflates the two. If an implementer treats the idempotency check as applying to the rebuilt EG rows (which have fresh recordNames and nil pointers), the check passes correctly. BUT if the sim Check 2 tests unmerge(keepLink:true) + pointer clear + re-merge WITHOUT rebuilding with a new recordName (i.e., reusing the same EG.recordName with cleared pointers), the idempotency check would see mergedIntoWeekID=nil and NOT skip — also correct. The deeper risk: a CloudKit delivery where the old EG tombstone and the new EG both arrive, and the merger's preferLive picks the stale non-nil pointers from the tombstoned old EG over the new EG's nil pointers (higher modifiedAt on the old one). The blueprint does not address what happens when an old EG with stale pointers conflicts with the new EG (same recordName is impossible since it's a hard delete+re-create in prod, but in CloudKit the EG uses a new CKRecord, so no conflict — this is safe). The sim Check 2 must use a NEW EventGroceryItem recordName for the rebuild step to match prod semantics. — *FIX:* Make the sim Check 2 setup explicit: after unmerge(keepLink:true), create a NEW EventGroceryItem value (different recordName) with totalQuantity=3. Call mergeEventIntoWeek with the new EG. Assert eventQuantity==3. Add a headless test that exercises the same: unmerge clears old EG pointers, new EG (fresh recordName, nil pointers) merges correctly to eventQuantity==3, not 6.
- **[blocker]** Tombstone monotonicity is broken in the post-batch repair pass. The blueprint says: 'encode+save tombstoned (same — NOT deleteRecord)'. But it also says: 'run ConflictRepair.dedupeGrocery(items:eventLinks:)' where the input items comes from 'store.allRecords(), decode all records, filter by type+weekID'. ConflictRepair.swift:32 filters out tombstones BEFORE deduplication ('let live = items.filter { !$0.isUserRemoved }'). However, if a peer device concurrently hard-deletes a row (CKSyncEngine delivers a deletion event), store.removeRecord removes it from the local store. On the NEXT batch that hits the same weekID, the post-batch repair iterates the surviving live rows — the deleted row is gone, its tombstone was never written. A device that hasn't seen the hard-delete will re-create the row from its own pending saves. The blueprint says 'tombstone losers; NEVER deleteRecord' for the dedupe output, which is correct. But it does not address the scenario where the INCOMING batch (via changes.deletions) removes a GroceryItem that the repair pass just saw as a keeper — the repair would mark a different row as loser/tombstone, then the incoming deletion removes the keeper. This is an edge case but the blueprint's repair-pass loop runs 'after the deletions loop' which is AFTER store.removeRecord already fired. The repair pass therefore sees an inconsistent set: some rows were modified (still present), some were deleted (gone from store), and it deduplicates only the survivors — which may produce a different keeper than the server intended. The needsResave guard ('compare before enqueue, same needsResave pattern') helps avoid no-op re-saves, but the loser selection can still diverge across devices if deletion order varies. — *FIX:* Document this as an acknowledged edge case. The repair pass must capture the affectedWeekIDs set BEFORE the deletions loop, and must treat any GroceryItem whose recordID is in changes.deletions as already-tombstoned (not absent) when building the sibling set for deduplication. Alternatively, enqueue a tombstone save for any hard-deleted GroceryItem that passes the isEventOnly + no-user-investment test, rather than letting store.removeRecord remove it silently. Add a test case: peer deletes a keeper GroceryItem; local device runs repair; assert the surviving rows do not produce a zombie double-count.
- **[high]** eventQuantity writer-ownership conflict: FieldMergeResolver.merge(EventGroceryItem) applies mergeEventQuantity (prefer non-nil, take max) on the EXISTING eventQuantity field. But after the value-type expansion, EventGroceryItem gains a NEW totalQuantity field that is the source of the contribution. The blueprint correctly distinguishes them. However, EventGrocerySyncMerger calls FieldMergeResolver.merge(_ a: EventGroceryItem, _ b: EventGroceryItem) which — per FieldMergeResolver.swift:51-63 — only merges the OLD four fields (recordName, mergedIntoGroceryItemID, mergedIntoWeekID, eventQuantity, modifiedAt). After the value-type expansion adds 11 new fields (totalQuantity, ingredientName, etc.), the resolver does NOT merge them — they fall to the LWW base winner (the higher modifiedAt side's values). This is stated as correct in the blueprint ('totalQuantity and ingredient identity fields are LWW'). BUT the needsResave comparison ('merged != remoteValue') now compares the FULL expanded EventGroceryItem value type including all 11 new fields. If the local copy has any new field set and the remote copy has it nil/empty (e.g., local was written before the ingredient name was populated by aggregation), the merger will enqueue a resave on EVERY fetch even when the sticky fields match — potentially causing ping-pong saves. This is not a correctness issue for the sticky invariants but IS a liveness issue (infinite resave loop is the risk named in Risk #6). — *FIX:* The needsResave comparison for EventGrocerySyncMerger should compare only the STICKY fields (eventQuantity, mergedIntoGroceryItemID, mergedIntoWeekID) rather than the full value type equality. Alternatively, document that the LWW base winner for ingredient identity fields is the higher-modifiedAt side, so if local has newer modifiedAt, those fields will be written back once and then stabilize. The sim check should assert that after convergence, no further pending saves are queued (hasPendingRecordChanges == false) to catch the ping-pong case.
- **[high]** Check-state triple tearing in the GroceryItem post-repair path. The blueprint's repair pass encodes keepers and tombstoned rows via GroceryCodec and enqueues as .saveRecord. GroceryCodec.encode correctly writes the CheckState triple as three separate fields (isChecked, checkedAtClock, checkedBy). But ConflictRepair.dedupeGrocery:68 merges check states from losers onto the keeper with: 'if loser.check.isChecked && !keeper.check.isChecked { keeper.check = loser.check }'. This IS atomic (it assigns the whole CheckState struct) — correct. However, if the keeper's encoded save arrives at the server AFTER a concurrent device has independently updated only isChecked (without updating checkedAtClock), the server merge via FieldMergeResolver will correctly use the check.at clock to pick the winner triple. The risk is NOT in the merger — it is in the repair pass's needsResave logic: the blueprint says 'compare before enqueue, same needsResave pattern' but does not specify that the FULL CheckState is compared atomically. If an implementer compares only individual fields (isChecked vs isChecked), they can produce a spurious resave that carries mismatched isChecked/checkedAtClock/checkedBy. This is subtle because the GroceryCodec.decode reconstructs the triple correctly — the risk is only if someone optimizes the needsResave check to compare field-by-field rather than value-type equality. — *FIX:* Explicitly state in the LAYER 3 wiring description that needsResave for GrocerySyncMerger (and the repair pass) uses full GroceryItem value-type equality (merged != remoteValue), which already works correctly because CheckState is Equatable as a struct. Add a comment in GrocerySyncMerger pointing to CheckState's atomic Equatable conformance as the guard against tearing. Low effort, prevents a future optimization regression.
- **[high]** ISO-8601 date ordering invariant for _resolve_target_week is stated in the blueprint but not enforced at the value-type boundary. Week.weekStart is currently String in Models.swift:129 with NO format enforcement. The new weekEnd field adds the same String type. EventMergeEngine._resolve_target_week compares weekStart <= eventDate and weekEnd >= eventDate as string lexical comparison. If ANY caller creates a Week with weekStart in a non-YYYY-MM-DD format (e.g., 'July 4, 2026', or 'M/D/YYYY', or an empty string), the comparison silently mis-resolves the target week with NO runtime error. The blueprint says 'WeekCodec enforces this on encode' but the value type itself and the pure EventMergeEngine have no guard. A Week created in tests without going through WeekCodec (the typical unit-test path — pure, no CloudKit) can have any weekStart format. — *FIX:* Add a runtime assertion or validation in Week.init (or a computed property weekStartDate: Date? for safe access) that weekStart matches YYYY-MM-DD format when non-empty. Alternatively, add a dedicated date-ordering test in EventMergeEngineTests that explicitly creates a Week with YYYY-MM-DD strings and asserts that lexical comparison matches the expected date ordering, making the invariant visible in the test file. The blueprint's headless test list does not include this — add it explicitly.
- **[high]** unmerge nil-target guard is correctly specified (event_grocery.py:493) BUT the blueprint's spec of what to do with the pointer AFTER the nil-target case is ambiguous. The blueprint says 'skip the subtraction but still clear the ev-row pointers'. The prod code (event_grocery.py:520-521) clears ev_row.merged_into_week_id and merged_into_grocery_item_id AFTER the if-target block — they are always cleared regardless of whether target was nil. This is correct. But in the CloudKit value-type port, clearing the pointers on an EventGroceryItem whose target was absent means the EventGroceryItem will be saved with nil pointers even though the GroceryItem never had its eventQuantity decremented. If the missing GroceryItem later arrives (out-of-order delivery), there is no mechanism to re-trigger the subtraction — the EG row's pointers are already nil. This is the same cross-aggregate atomicity race the blueprint acknowledges in its risk list but offers no resolution for. The blueprint's sim poll-until-converged guard handles the detection side but not the repair side. — *FIX:* Document this as an explicit known limitation: if an EventGroceryItem arrives before its target GroceryItem, the unmerge pointer-clear creates a permanently un-accounted eventQuantity on the GroceryItem (the EG can't re-trigger the subtraction after pointers are nil). The mitigation is: after the GroceryItem arrives (in the post-batch repair pass), if the GroceryItem has eventQuantity > 0 but NO EventGroceryItem points to it (mergedIntoGroceryItemID == this GroceryItem's recordName), and the GroceryItem isEventOnly, consider it stale and self-delete (tombstone). Add this as a ConflictRepair.nullingDangling analog for the eventQuantity direction.
- **[medium]** M68 repoint path breaks tombstone monotonicity for REPOINTEDLINKS. ConflictRepair.dedupeGrocery repoints EventGroceryItem links from loser to keeper. The blueprint encodes repointedLinks via EventGroceryItemCodec and saves+enqueues them. These EventGroceryItem records may have their mergedIntoGroceryItemID changed from loser.recordName to keeper.recordName. But the deduplication output does NOT change the loser's recordName — it tombstones the loser GroceryItem. After the repoint, an EventGroceryItem that previously pointed to the loser now points to the keeper. If a concurrent device's FieldMergeResolver.merge(EventGroceryItem) resolves the pointers via preferLive — preferring the NON-NIL pointer over nil — and that peer had cached the OLD pointer (to the loser), the merge will RESTORE the pointer to the (tombstoned) loser. This is because preferLive picks non-nil by default when one side is nil. Post-repoint, the repair-pass write carries a non-nil (new) pointer; a peer that hasn't fetched the repair-pass write yet may resolve with the old non-nil pointer to the loser via preferLive — both sides are non-nil, so preferLive falls to modifiedAt tiebreak. If the old pointer write has a higher modifiedAt, the tombstoned loser's ID wins. — *FIX:* After tombstoning the loser GroceryItem (isUserRemoved=true) AND repointing its EventGroceryItem links, bump the modifiedAt clock on the repointed EventGroceryItem to a value HIGHER than any modifiedAt on the old pointer write. This ensures preferLive's tiebreak always picks the repointed (new) value. The dedupeGrocery function should set repointedLink.modifiedAt = max(all involved modifiedAt values) + 1. Add a test: two concurrent dedup results with different pointer values; after merge, the higher-modifiedAt repoint wins.
- **[medium]** _matchKeys in EventMergeEngine uses unit as a tuple element in BOTH base_key and name_key, and the blueprint's port is: baseKey = '(baseIngredientID)\u{1}(unit)' when baseIngredientID non-empty, else '\u{1}(unit)\u{1}(normalizedName)'. The prod _match_keys returns tuples of 3 elements: base_key = (base, unit, '') if base else ('', unit, norm); name_key = (norm, unit, ''). The Swift port uses \u{1} as a delimiter — but the CKDSL normalizeUnit call in prod (event_grocery.py:304: 'normalize_unit(getattr(row, 'unit', '') or '')') normalizes the unit before keying. The EventMergeEngine in Swift has NO normalize_unit equivalent. If an EventGroceryItem arrives with unit='LB' (uppercase) and the week GroceryItem has unit='lb' (lowercase), _matchKeys in Swift will NOT match them despite being the same ingredient. The prod code normalizes both sides before building the key. The blueprint never mentions this — and Models.swift has no normalize_unit function. — *FIX:* The Swift _matchKeys implementation must normalize the unit string to lowercase (at minimum) before building the key, mirroring what prod does via normalize_unit. Add a headless test for unit case-mismatch: EG with unit='LB', week GroceryItem with unit='lb' — assert they MATCH (same ingredient). The blueprint's test list only covers 'unit mismatch guard' (lb vs oz) — add case-normalization coverage.
- **[medium]** The isEventOnly check in unmerge (target.sourceMeals.hasPrefix('event:')) is a PREFIX check — correctly ported in the blueprint. But ConflictRepair.dedupeGrocery ALSO uses a different isEventOnly definition on GroceryItem (Models.swift:86-89): 'guard let eq = eventQuantity, eq > 0 else { return false }; return sourceMeals.hasPrefix("event:")'. The post-batch repair pass calls dedupeGrocery AFTER an unmerge that sets eventQuantity to nil (or 0) on the target. After the unmerge, target.eventQuantity is nil or 0, so GroceryItem.isEventOnly returns FALSE in Models.swift — even though sourceMeals still has the 'event:' prefix. This means the ConflictRepair path that calls dedupeGrocery after an unmerge will NOT self-delete the now-zero-eventQuantity event-only row via the deduplication path, because the keeper policy and self-delete in dedupeGrocery is NOT the same logic as unmerge's self-delete. The unmerge's self-delete is in EventMergeEngine, not in dedupeGrocery. The repair pass should NOT be expected to self-delete event-only rows — that is the EventMergeEngine's job. This is acceptable, but the blueprint's statement 'run ConflictRepair.dedupeGrocery' in the repair pass may confuse an implementer into expecting the repair to also clean up post-unmerge event-only rows. — *FIX:* Clarify in the LAYER 4 repair-pass spec that dedupeGrocery handles STRUCTURAL duplicates (two GroceryItem rows with the same normalizedName+unit in one week), NOT semantic cleanup after unmerge. Post-unmerge self-delete is exclusively in EventMergeEngine.unmergeEventFromWeek via deletedGroceryItemNames. The repair pass encodes these deletions as tombstones (isUserRemoved=true) and saves them — it does NOT call dedupeGrocery for that purpose. This distinction prevents an implementer from removing the self-delete logic from EventMergeEngine thinking dedupeGrocery covers it.
- **[medium]** Event.linkedWeekID in the CKDSL schema (phase0-schema.ckdb:198) is already declared as STRING — correct for a soft pointer. But the blueprint's EventCodec spec says 'linkedWeekID as plain String'. The Event Swift struct (Models.swift:142) has 'linkedWeekID: String?' — optional. The codec must encode nil as absent (no key) and decode absent as nil. However, the Phase 2b CKDSL Event block does NOT include modifiedAtClock — and the blueprint correctly notes this must be added additively. Critically, the Phase 2b Event block also does NOT include autoMergeGrocery or manuallyMerged as separate fields — but the CKDSL at line 194-195 DOES have 'autoMergeGrocery INT64' and 'manuallyMerged INT64'. Good — those exist. But the CKDSL does NOT have linkedWeekID — wait, line 198 shows 'linkedWeekID STRING'. So the existing Event block in the CKDSL has linkedWeekID but NOT modifiedAtClock. The blueprint's additive field (modifiedAtClock INT64 on Event) is correct and necessary. However, the FieldMergeResolver.merge(Event) at FieldMergeResolver.swift:76-83 reads modifiedAt from BOTH events to select the winner — if modifiedAtClock is absent from the CKDSL and not stored, EventCodec.decode will return modifiedAt=0 for all Events, making the clock-based LWW base selection random (always picks 'a' when equal). This means manuallyMerged sticky-OR still works (correct), but the LWW base for other fields (name, eventDate, autoMergeGrocery) will be arbitrary on the first merge before modifiedAtClock is in the schema. — *FIX:* This is already correctly called out in the blueprint as a required additive schema field. Flag it as a deployment ordering dependency: the sim Check 4 (pin survives concurrent edit) is ONLY valid after the schema deploy includes modifiedAtClock on Event. Without it, the check may pass spuriously (both events have modifiedAt=0, tie goes to 'a', which may coincidentally be the right winner). Make this a pre-condition checkpoint in the sim check.
- **[medium]** The blueprint's apply_auto_merge_policy Branch 2 re-date detection uses string comparison: 'eventDate moved off linked week's [weekStart, weekEnd] range'. The port says: 'if linkedWeekID set AND eventDate non-empty AND eventDate moved off linked week's [weekStart, weekEnd] range: call unmergeEventFromWeek(keepLink: false)'. But when unmergeEventFromWeek is called with keepLink:false, it clears event.linkedWeekID. Then the blueprint says 'then resolveTargetWeek, then mergeEventIntoWeek into the new target'. resolveTargetWeek checks event.linkedWeekID FIRST — 'if event.linkedWeekID is non-nil and non-empty, return the matching week'. But linkedWeekID was just cleared by unmerge(keepLink:false). So resolveTargetWeek falls through to the date-range query — correct. However, this means the event's linkedWeekID is nil BETWEEN unmerge and merge. If the EventMergeEngine returns EventMergeResult and the caller saves all changed records as a batch, the Event will transiently have linkedWeekID=nil before the re-merge sets it to the new week's recordName. In a CloudKit multi-device scenario, if the peer fetches the Event between the unmerge-save and the re-merge-save, it will see manuallyMerged=false (correct) and linkedWeekID=nil — and may decide to not merge at all on its next applyAutoMergePolicy call. The result is a stuck unmerged state on the peer. — *FIX:* The re-date branch of applyAutoMergePolicy must be transactionally atomic from the CloudKit perspective. The blueprint's EventMergeResult correctly returns BOTH the unmerge changes (cleared EG pointers) AND the re-merge changes (new EG pointers, updated event.linkedWeekID) in a single result. The caller MUST enqueue ALL of these as a single batch of .saveRecord operations before calling syncEngine.sendChanges(). The blueprint does not specify this. Explicitly state in the LAYER 4 wiring: 'the EventMergeResult from applyAutoMergePolicy must be fully encoded and enqueued before sendChanges() to avoid a transient nil-linkedWeekID state on the server'.
- **[low]** The 0.0001 floating-point threshold in unmerge is correctly ported (set eventQuantity=nil when post-subtraction <= 0.0001). But the blueprint's roundedSum helper rounds to 2 decimal places — if eventQuantity was 3.00 and totalQuantity was 3.00, the subtraction yields 0.00, which is <= 0.0001, so it's nil'd. Correct. But if floating point gives 3.00 - 3.00 = 2.842e-14 (a common FP residual), 2.842e-14 <= 0.0001 is TRUE — also nil'd. Correct. However, if totalQuantity was serialized through the codec as a Double and gained a small FP error (3.0000000001), the subtraction could give -1e-10, which is <= 0.0001 — nil'd, correct. But the converse: if eventQuantity is 3.001 (due to an earlier roundedSum adding 1.5 + 1.5 + 0.001) and totalQuantity is 3.001, the subtraction = 0.000 — nil'd. All these cases are correct. The only failure case is if the 0.0001 threshold is applied BEFORE the round-to-2dp, producing different results than prod. The blueprint says 'port the 0.0001 threshold exactly' — confirm it is applied on the RAW subtraction result, not after rounding to 2dp. — *FIX:* Confirm in the implementation note that the 0.0001 threshold check in unmergeEventFromWeek is applied to the raw (Float) subtraction result BEFORE any rounding, matching event_grocery.py:496 exactly. The roundedSum helper (which rounds to 2dp) is used for MERGING quantities, not for unmerge subtractions — the blueprint correctly separates these but this should be called out to prevent accidental roundedSum usage in the unmerge path.
- **[low]** dedupeGrocery tombstones losers: already verified as correct in ConflictRepair.swift:77-80 (sets isUserRemoved=true, never calls session.delete). However, the prod unmerge in event_grocery.py:519 calls session.delete(target) for self-deleted event-only rows — this is a HARD delete in the Python path. The blueprint says the Swift port should emit deletedGroceryItemNames from unmergeEventFromWeek, and the engine repair pass tombstones these instead of hard-deleting. The asymmetry is intentional and correct for CloudKit (tombstones survive peer re-creation). But the blueprint's unmerge self-delete test (sim Check 3) says 'G is tombstoned (isUserRemoved=true) OR absent'. The 'absent' branch should NOT be reachable if the blueprint is implemented correctly — only tombstoned should be the outcome. The 'OR absent' wording leaves room for a hard-delete implementation to pass the test incorrectly. — *FIX:* Change sim Check 3 assertion to 'G is tombstoned (isUserRemoved=true)' WITHOUT the 'or absent' fallback. An absent G (hard-deleted) would allow a peer that hasn't seen the delete to re-create it from a stale pending save — the exact failure mode tombstones prevent. The test should assert tombstone, not absence.
- **[low]** The blueprint's EventGroceryItem value type expansion adds eventQuantity as an EXISTING field and totalQuantity as a NEW field. The FieldMergeResolver.merge(EventGroceryItem) at FieldMergeResolver.swift:51-63 applies mergeEventQuantity on eventQuantity (correct). After the expansion, the struct has both fields. The blueprint's comment 'eventQuantity (existing) is the resolver's sticky field; totalQuantity (new) is the event row's own contribution' is clear. But in the prod EventGroceryItem model (event.py:213-254), there is NO eventQuantity column at all — the prod model has only total_quantity. The Swift EventGroceryItem.eventQuantity field was introduced in Phase 4 as part of the cross-aggregate pointer semantics for the CloudKit port. The blueprint correctly separates them, but the CKDSL for EventGroceryItem must include BOTH fields: eventQuantity DOUBLE (the resolver's sticky field) AND totalQuantity DOUBLE (the event row's contribution). The blueprint's schema additions list includes both. Confirm that the CKDSL EventGroceryItem block includes eventQuantity as a separate field from totalQuantity — this is correct per the blueprint, just needs explicit verification that the implementer doesn't mistake 'prod has only total_quantity' for 'the CloudKit schema needs only totalQuantity'. — *FIX:* Add a comment in the CKDSL EventGroceryItem block (phase0-schema.ckdb) explicitly noting that eventQuantity is a CloudKit-only field added for the resolver's sticky semantics and does NOT exist in the prod SQL schema. This prevents an implementer from 'porting the schema from prod' and dropping eventQuantity as 'not in prod'.

### Review — Adversarial review of spec completeness, correctness, and implementation risk (approved=False)

- **[BLOCKING]** LAYER 0 value-type expansion: EventGroceryItem field addition has a semantic collision that the blueprint acknowledges but does NOT correctly specify. EventGroceryItem.eventQuantity (existing) is the resolver's sticky field (FieldMergeResolver.swift:54 merges via mergeEventQuantity, preferring non-nil, taking max). The blueprint says new EventGroceryItem.totalQuantity is 'the event row's own contribution that EventMergeEngine reads when adding to week GroceryItem.eventQuantity.' However, production event_grocery.py:342-348 shows 'if ev_row.total_quantity is None: ... set mergedIntoWeekID only, no numeric add' — text-only rows (no quantity). The blueprint's design makes totalQuantity 'the' field for the merge function, but the existing resolver fixture (Models.swift + FieldMergeResolver:51-54) is locked to eventQuantity-only semantics. The blueprint MUST specify: (a) does FieldMergeResolver.merge(EventGroceryItem) CHANGE to use totalQuantity? (no — it's sticky text-join state, NOT LWW), OR (b) are both fields managed separately with no resolver cross-talk (totalQuantity is LWW, eventQuantity stays sticky), with a clear comment at the point the fields diverge? Current spec leaves this ambiguous — the codec section says 'totalQuantity ↔ CKRecord[totalQuantity] ... eventQuantity ↔ CKRecord[eventQuantity] stays separate' (correct), but the EventMergeEngine implementation will read both fields in unmerge (event_grocery.py:493 'if target is not None and ev_row.total_quantity is not None') without being told which is the merge-safe one. — *FIX:* Clarify in LAYER 0 (Models.swift change): add an inline doc comment above the new EventGroceryItem.totalQuantity field: 'The event row's own aggregated quantity contribution (LWW semantics — set once during aggregation, read by EventMergeEngine). Distinct from eventQuantity (the sticky merge field resolved by FieldMergeResolver.mergeEventQuantity). DO NOT conflate in the resolver or in merge-decision points.' Then in EventMergeEngine.swift, document at the top of unmergeEventFromWeek: 'Reads totalQuantity (the event contribution) to reverse the prior merge; eventQuantity is owned by the resolver (mergeEventQuantity) and must NEVER be written by this function.'
- **[BLOCKING]** LAYER 1 — EventMergeEngine._matchKeys implementation: the blueprint correctly specifies the port but the load-bearing syntax is underspecified. Production event_grocery.py:301-305 has 'base = getattr(row, "base_ingredient_id", None) or ""' which uses Python's 'or' (falsy: None, "", 0, [], {}) to coerce None → "". The blueprint says 'baseKey = "(baseIngredientID)\u{1}(unit)" when baseIngredientID is non-empty, else "\u{1}(unit)\u{1}(normalizedName)"' — correct LOGIC but SYNTAX is missing a guard. Swift: 'baseIngredientID ?? ""' (nil to empty), but then must check '.isEmpty' (the 'non-empty' condition), NOT just truthiness. Same for normalizedName — it can be an empty string legitimately. The blueprint's pseudocode does not show Swift null-coalescing + isEmpty pattern, so an implementer might write 'if baseIngredientID { ... }' (crashes on optional unwrap) or 'if (baseIngredientID ?? "").isEmpty' (clumsy). This is a REAL trap for Swift — the optional type system is not Python getattr. — *FIX:* Add explicit Swift guard pattern to the blueprint's _matchKeys section: 'let base = baseIngredientID ?? "" (coalesce nil to empty); let unit = unit (never nil per normalization, but may be empty); let norm = normalizedName ?? "" (coalesce nil to empty); if !base.isEmpty { return base + "\u{1}" + unit } else { return "\u{1}" + unit + "\u{1}" + norm }. For nameKey, always return "\u{1}" + unit + "\u{1}" + norm.' Also note: production uses normalize_unit (a function call); the blueprint defers normalization to the codec. Make EXPLICIT: are weekGroceries and eventLinks ALREADY normalized on read from the store, or does EventMergeEngine normalize them on entry? If deferred, the keyring function must normalize BOTH keys in parallel (event row's unit must match week row's unit AFTER normalization, or mismatches slip through).
- **[BLOCKING]** LAYER 1 — mergeEventIntoWeek week-index de-duplication trap: production event_grocery.py:350-352 does 'ev_base_key, ev_name_key = _match_keys(ev_row); match = week_index.get(ev_base_key) or week_index.get(ev_name_key)' — tries base_key FIRST (exact catalog match wins), else falls back to name_key (name-only match). The blueprint says 'Build a week index keyed by both _matchKeys outputs (same row may appear under two keys).' This is CORRECT and necessary for M22.2 (event row catalog-resolved ↔ week row not resolved, or vice versa). However, the blueprint does NOT specify the lookup ORDER or conflict resolution. If a week has TWO rows under the same key (deduplication failure pre-landing in EventMergeEngine), which one do we match the event row to? The blueprint does not say "assume input is dedupe'd" or "take the first / last / lowest recordName". Production can run dedupe_week_grocery before merge_event_into_week; CloudKit will not have that guarantee (dedupe runs POST-batch, after the merge). This is a SEQUENCING risk — if EventMergeEngine merges an event into a week BEFORE ConflictRepair.dedupeGrocery runs (the post-batch pass), the event might add to the wrong copy of a duplicate, then the merge hook re-points the link away. The blueprint's post-batch repair pass calls dedupeGrocery AFTER collecting affectedWeekIDs from modified records (line: 'for each weekID in affectedWeekIDs'). But a NEWLY CREATED event-only GroceryItem (created by mergeEventIntoWeek during a fetch on one device) arrives BEFORE the dedup can run. Two devices doing simultaneous merges of the same event into the same week = two event-only GroceryItems created, identical keys, pre-dedupe. When the dedupe runs, it collapses them; EventGroceryItem links are repointed. But if the links point to DIFFERENT event-only rows (device-A's and device-B's), and the dedupe picks one as keeper, the repoint loses device-B's contributions. — *FIX:* Add to the blueprint—new subsection in LAYER 1: 'IDEMPOTENCY & PRE-MERGE DEDUPE ASSUMPTION: EventMergeEngine assumes the input weekGroceries are already dedupe'd by normalizedName+unit (the MergeKey categories). A NOT-DEDUPE'd week arriving from a fetch is a data-corruption failure, not a transient race. The engine WILL add contributions to the wrong copy of a duplicate if the input is corrupt. Production runs dedupe BEFORE merge in a monolith; CloudKit must RUN DEDUPE FIRST if weekGroceries are being passed to the merge. This is a PRE-CONDITION CONTRACT. Implement as: (a) add an assertion at mergeEventIntoWeek entry: "live week GroceryItems have been dedupe'd by ConflictRepair.dedupeGrocery"; (b) mark the post-batch repair hook's dedupeGrocery call BEFORE the mergeEventIntoWeek call would be valid (it's not — the hook runs AFTER changes land). REVISED SEQUENCING: (1) fetch + applyRemoteModification lands all records, (2) run ConflictRepair.dedupeGrocery over the week, (3) [NOT DONE IN ENGINE] re-run merges if they were pending. Caveat: this breaks the current post-batch design (dedupe runs once, after all modifications). Either: accept that EventMergeEngine works on pre-deduped input ONLY, or move mergeEventIntoWeek into the merger seam (Phase 4 RecordMerger, not post-batch). The blueprint's current design (post-batch dedupe, then implicit re-merge via the merger seam on conflict) is a LOOP RISK.'
- **[BLOCKING]** LAYER 2 — Codec field type mapping ambiguity for date fields: the blueprint specifies 'eventDate: String (encode as CKRecord Date from YYYY-MM-DD, decode back to string)' and 'weekStart/weekEnd: String (encode as CKRecord Date from YYYY-MM-DD, decode back to string)' but does NOT specify error handling. What happens if the Date parse FAILS (corrupt/invalid YYYY-MM-DD on the local record, or a record from an older version with a different format)? Production Python does '2026-06-01' format validation upfront (ISO-8601 YYYY-MM-DD is enforced at the model layer). CloudKit has no validation at the value-type level (Swift Date is a runtime type). The blueprint says 'encode eventDate as CKRecord Date from YYYY-MM-DD (if non-empty), or nil when empty-string sentinel' — but what if `eventDate = "06/01/2026"` (US format, stale data)? The codec's parse will throw. Does it throw, crash, or return nil? The blueprint does NOT specify exception handling or fallback. This is a DATA MIGRATION risk — if any event_date in production is NOT YYYY-MM-DD, the codec will crash. — *FIX:* Add to LAYER 2 codec section: 'eventDate/weekStart/weekEnd parsing (error handling): (a) Use Swift's ISO8601DateFormatter with `.withFractionalSeconds` to parse YYYY-MM-DD. (b) On parse failure, log an error and encode as nil (treat as missing date). (c) Add a migration step before Phase 5 deploy: scan the backend's Event.event_date and Week.week_start/week_end columns for non-YYYY-MM-DD values; raise an error if any are found. (d) In the codec, add a line comment: "Empty string or nil are equivalent; non-empty must be YYYY-MM-DD per ISO-8601. Stale formats from pre-Phase-5 are treated as data corruption (logged, encoded as nil)."'
- **[HIGH]** LAYER 3 — DispatchingMerger and multi-type merger wiring: the blueprint says 'Option (b) create a DispatchingMerger wrapper that holds [RecordMerger] and delegates' and 'eventRef parent as .deleteSelf CKReference.' But it does NOT specify how the engines's merger is WIRED at initialization. Current code (HouseholdSyncEngine.swift:28) has 'public var merger: RecordMerger?' — set by the caller. The blueprint does not say whether the engine should auto-wire a DispatchingMerger on init, or if the caller is responsible. The sim check in simVerify says 'configure both engines with a DispatchingMerger([GrocerySyncMerger(), EventGrocerySyncMerger(), EventSyncMerger()])' — this means the CALLER (the sim) wires it. But which code path wires it in PRODUCTION? Does the app (not the test) do `engine.merger = DispatchingMerger(...)`? This is not specified. Without explicit initialization, production might ship without any merger, and both event merge and already-working grocery merge silently fail to resolve conflicts. — *FIX:* Add to the blueprint a new subsection in LAYER 3: 'Merger initialization contract: HouseholdSyncEngine does NOT auto-wire the merger on init (keeps the engine decoupled). The CALLER (e.g., HouseholdZoneManager or AppDelegate) is responsible for setting engine.merger = DispatchingMerger([GrocerySyncMerger(), EventGrocerySyncMerger(), EventSyncMerger()]) before the first sync(). Add a runtime assertion to HouseholdSyncEngine.handleEvent (fetch handler): `precondition(merger != nil, "HouseholdSyncEngine requires a merger to be set before sync")` so any missed wiring fails loudly in debug, not silently in production.'
- **[HIGH]** LAYER 4 — Post-batch repair hook placement and change-detection: the blueprint says 'Inside case .fetchedRecordZoneChanges, after the modifications loop and deletions loop complete, ... Collect affectedWeekIDs: Set<String> by decoding each modified record during the loop — if recordType == GroceryCodec.recordType, read the weekRef field.' But weekRef is a CKReference on GroceryItem (added in the schema changes, not yet in code). The blueprint does NOT specify: (a) how to READ a CKReference from a CKRecord if the value is not always present (upgrade-safe), (b) whether to filter tombstoned records (isUserRemoved=true) from the dedupeGrocery input (the dedupeGrocery function does filter, per ConflictRepair.swift:32, but the blueprint does not say this explicitly to the hook implementer). (c) whether to call dedupeGrocery even when affectedWeekIDs is empty (safe, no-op; but unnecessary). The blueprint's language is 'for each weekID in affectedWeekIDs: call store.allRecords(), decode and filter by type+weekID' — this is a full store scan PER weekID, which is O(N*W) (N=store records, W=affected weeks). At a household scale, fine; but this is NOT documented as a cost, so an implementer might think it's a cheap operation. — *FIX:* Clarify in LAYER 4: '(a) CKReference reading: use `record["weekRef"] as? CKReference` (optional), then access `.recordID.recordName`. If absent, skip (upgrade safety). (b) Tombstone filtering: merge pre-filters via store.allRecords().filter { !$0.isUserRemoved }, same as ConflictRepair.dedupeGrocery (line 32). Do NOT pass tombstones to dedupeGrocery; they are outputs, not inputs. (c) Cost: O(N*W) full-store decode per week — acceptable at household scale (<10k records). Profile if W >> 100. (d) Guard against empty affectedWeekIDs: wrap the loop in `guard !affectedWeekIDs.isEmpty else { return }` to skip the repair entirely if no changes touched grocery records.'
- **[HIGH]** SCHEMA additions: the blueprint specifies 'Update RECORD TYPE GroceryItem (already in schema at line 314) — add TWO additive fields: weekRef REFERENCE (CASCADE parent — Week→GroceryItem is CASCADE per week.py:188), storeLabel STRING.' But phase0-schema.ckdb (current read) does NOT have GroceryItem in it yet (only at line ~95+ is Phase 2b). The blueprint is correct that Phase 4 added GroceryItem, but the LINE NUMBER REFERENCE is STALE. This is a documenation bug that will cause confusion during deployment. Additionally, the blueprint says 'eventRef parent as .deleteSelf CKReference' for EventGroceryItem, but does NOT specify the full CKReference syntax in CKDSL (it should be 'REFERENCE ... ON DELETE SET NULL' or 'REFERENCE QUERYABLE' depending on CloudKit version). CKDSL syntax is terse and the blueprint does not show it, so an implementer will have to grep existing records to see the pattern. — *FIX:* Correct the blueprint's schema section: (a) Remove stale line numbers — refer to 'the existing GroceryItem block in phase0-schema.ckdb (Phase 4)' instead of 'line 314'. (b) Show FULL CKDSL syntax for the eventRef CKReference: 'event REFERENCE' (no ON DELETE — CloudKit defaults to SET NULL for cross-zone refs) or 'event REFERENCE ... CASCADE' (if same-zone cascade is needed per §C). Check phase0-schema.ckdb EXISTING pattern: Week→GroceryItem is 'week REFERENCE' (plaintext, no CASCADE marker visible in CKDSL because CASCADE is a property, not part of the field declaration in CKDSL v1). Clarify: does eventRef need CASCADE (Event delete → EventGroceryItem auto-delete) or SET NULL (soft pointer)? Spec §C says 'Event→{...Grocery,…}' is CASCADE, so it should be 'event REFERENCE' (implicit CASCADE via parent config). Make this EXPLICIT in the CKDSL template.
- **[HIGH]** simVerify test design: the blueprint's simVerify specifies 'Check 2 — 3→6→9 guard (the headline invariant from spec §5 Verify): on engineA, simulate regenerate: call unmergeEventFromWeek(keepLink:true) (clears EG pointers, subtracts from G), rebuild EG (new EventGroceryItem with totalQuantity=3), call mergeEventIntoWeek again.' But the simVerify does NOT define WHAT "rebuild EG" means. Does it create a NEW EventGroceryItem (new recordName, same totalQuantity=3), or does it mutate the existing one? If new, then you have TWO EventGroceryItem records pointing to the same week GroceryItem after rebuild, and dedupeGrocery must HANDLE that collision (which it doesn't — dedupeGrocery is for GroceryItem de-duplication, NOT for EventGroceryItem cross-links). The production flow is: regenerate_event_grocery (called after a meal edit) deletes the old EventGroceryItems and creates new ones with fresh aggregation. The blueprint does NOT specify whether EventMergeEngine is IDEMPOTENT on the same EventGroceryItem record (unmerge + re-merge with same recordName), or if regenerate implies delete+create. This is a SEMANTIC gap that makes the test unverifiable — an implementer cannot know if the test is valid until they understand what regenerate-in-CloudKit means. — *FIX:* Clarify the simVerify Check 2: '"rebuild EG" means: unmerge clears the EG pointers + subtracts from G. Then, to simulate a client-side meal edit → regen, DELETE the old EventGroceryItem (call engine.delete(EG.recordID)), create a NEW EventGroceryItem with fresh totalQuantity=3 (call engine.save(newEG)), and sync. This matches production: regenerate_event_grocery (event_grocery.py:238) calls session.delete(ev_row) then session.add(new_row). After the new EG lands on engineB via fetch, mergeEventIntoWeek is called AGAIN by applyAutoMergePolicy (assuming auto_merge_grocery=true and a target week exists). The convergence CHECK: engineB should see G.eventQuantity == 3 (not 6, not 9). If 6, the merge was called twice on the same EG. If 9, re-merge added to the stale EG from the prior merge without unmerge.' Add assertion: 'Assert that the old EG record no longer exists on either engine (tombstoned or deleted).''
- **[MEDIUM]** Models.swift field additions: the blueprint specifies adding defaulted parameters to EventGroceryItem and other value types, but does NOT specify the ORDER of parameters in the initializer. Swift initializers are positional until you use named arguments. If the blueprint adds new parameters at the END (totalQuantity, baseIngredientID, ...), then existing call sites that use positional arguments (e.g., EventGroceryItem(recordName: ..., mergedIntoGroceryItemID: ..., mergedIntoWeekID: ..., eventQuantity: ..., modifiedAt: 0)) will still work (new params are defaulted). But if any code BUILDS an EventGroceryItem by position, adding params in the middle will shift positions and break. The blueprint says 'Do not change any existing field names, types, or initializer parameter labels — only add new defaulted parameters' but does NOT specify THE POSITION in the param list. This is a COMPATIBILITY trap. — *FIX:* Clarify in LAYER 0: 'EventGroceryItem.init: add all new parameters at the END, after modifiedAt, in this order: totalQuantity, baseIngredientID, ingredientVariationID, ingredientName, normalizedName, unit, quantityText, category, sourceMeals, notes, reviewFlag, resolutionStatus. Each has a default value (see field declarations). This preserves positional-argument call sites. Same for Week.init: add weekEnd at the end. Verify: grep the codebase for EventGroceryItem(recordName: and Week(recordName: — if any use positional args past modifiedAt, they WILL BREAK if params are added mid-list. The grep should return zero (all call sites use named arguments or fill all params). If not zero, those call sites must be updated BEFORE the change lands.'
- **[MEDIUM]** EventCodec and date/bool encoding consistency: the blueprint specifies 'manuallyMerged/autoMergeGrocery as INT64 Bool (0/1 pattern from GroceryCodec)' but the existing GroceryCodec uses (item.isUserAdded ? 1 : 0) pattern (line 39-40). This is CORRECT Swift. However, the blueprint DOES NOT specify whether to use the SAME helper or define new ones. If EventCodec replicates the 0/1 conversion inline, it's boilerplate. If it factors into a helper, it's DRY. The blueprint does not guide this decision, so an implementer might copy-paste and miss an edge case (e.g., nil Bool being mishandled as 0 instead of nil). — *FIX:* Add to LAYER 2 codec section: 'Bool encoding pattern (reuse from GroceryCodec): in encode, use (value ? 1 : 0) as CKRecordValue for all Bool → INT64. In decode, use (record["fieldName"] as? Int ?? 0) != 0 to read back. This is the ONLY pattern used in HouseholdSync codecs; do NOT invent variants.'
- **[MEDIUM]** Headless test coverage: the blueprint lists 11 headless tests for EventMergeEngineTests, but does NOT specify the test input FIXTURES or INVARIANTS in detail. For example, 'EventMergeEngineTests: 3->6->9 guard — call mergeEventIntoWeek, then unmergeEventFromWeek(keepLink:true) + rebuild EventGroceryItem (new totalQuantity=3) + mergeEventIntoWeek again; assert changedGroceryItems.first.eventQuantity == 3 (not 6, not 9).' — but this does NOT specify: (a) what the INITIAL state is (new Event? new Week?), (b) what the first mergeEventIntoWeek produces (does it create an event-only GroceryItem, or match an existing one?), (c) how to VERIFY the changedGroceryItems result (is it in the EventMergeResult.changedGroceryItems list, or do you scan the returned GroceryItem's eventQuantity?). A test written from just this spec could be INVALID without catching the bug it's supposed to guard. The test names are good, but the SETUP and ASSERTION logic must be nailed down. — *FIX:* Flesh out the headless tests section with FIXTURES and ASSERTIONS for each of the 11 test cases. Example template: '**Test: 3→6→9 guard.** Fixture: Week W (weekStart="2026-06-01", weekEnd="2026-06-07"), GroceryItem G on W (totalQuantity=nil, eventQuantity=nil, recordName="g1"), Event E (eventDate="2026-06-03"), EventGroceryItem EG (totalQuantity=3.0, recordName="eg1", mergedIntoWeekID=nil, mergedIntoGroceryItemID=nil). Sequence: (1) Call EventMergeEngine.mergeEventIntoWeek(event: E, eventLinks: [EG], weekGroceries: [G]). Assert result.changedGroceryItems contains G with eventQuantity=3.0. (2) Unmerge via unmergeEventFromWeek(event: E, eventLinks: [EG], weekGroceries: [G], keepLink: true). Assert result.changedGroceryItems contains G with eventQuantity=nil. (3) Create newEG (totalQuantity=3.0, recordName="eg2"). Rebuild: delete old EG, add newEG, call mergeEventIntoWeek again. Assert G.eventQuantity == 3.0 (not 6.0, not 9.0). This is the FULL test flow.'
- **[MEDIUM]** On-sim verification poll-until-converged helper tolerance: the blueprint says 'The poll-until-converged helper must tolerate a transiently-dangling mergedIntoGroceryItemID (keep polling, not fail-fast).' But does NOT specify the TIMEOUT or RETRY LOGIC. The existing runHouseholdSyncCheck (in CloudKitDebugView.swift, which I have not read fully) has a polling mechanism. Does it have a hardcoded timeout? Does it log retries? If EventGroceryItem.mergedIntoGroceryItemID points to a GroceryItem that hasn't arrived yet (network reorder), the poll must NOT treat it as a failure state — it must be expected. But if the poll runs for 30 seconds and the dangling ref is NEVER resolved, the test should FAIL (real error, not transient race). The blueprint does not specify the pass/fail boundary. — *FIX:* Add to the simVerify section: 'Poll-until-converged helper contract: After each sync(), fetch fresh copies of all records from both engines and compare. If any EventGroceryItem.mergedIntoGroceryItemID points to a GroceryItem absent from weekGroceries on that engine, this is EXPECTED (transient delivery ordering). Continue polling (do NOT assert failure). If the GroceryItem is present AND the values do NOT match the assertion (e.g., G.eventQuantity != 3), record a failure and stop polling. Timeout: 2 seconds per poll, 10 retries (20 second total). If timeout is exceeded, fail the test with the last-observed mismatch. Log each poll retry with the seen state to aid debugging.'
