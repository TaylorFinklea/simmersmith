# Current State
Branch: main

## Plan
- [x] e0a P1 release: main through `4ff9934` pushed; Xcode 26.3 concurrency compatibility fix `fb34fae`; CI run 29437504939 green (packages, app build, 90 app-target tests); build 157 uploaded + `VALID`. Verify: `gh run view 29437504939 --json conclusion,jobs` and ASC `/v1/builds?filter[version]=157` report success/VALID.
- [x] `51d` schema promotion: EventAttendee.updatedAt added to `phase0-schema.ckdb` (`a1e71b5`) and deployed through authenticated CloudKit Dashboard; review showed 1 EventAttendee field, 0 indexes, 0 roles; Production view shows updatedAt TIMESTAMP. CK 513 + generic iOS build green. Two-device recency acceptance remains under Blockers.
- [x] **e0a P1a Lead spec/decomposition.** Added + GLM/MiniMax-adversarially revised `phases/e0a-shadow-mirror-spec.md`: scoped generation/WAL, state-coverage revision, canonical digest, durable asset rebinding, intent high-water, exact ack transitions, fencing, crash matrix, bounded TDD items. `tier_floor: lead` · `complexity: L`. Verify: `test -f .docs/ai/phases/e0a-shadow-mirror-spec.md && rg -q "Crash matrix" .docs/ai/phases/e0a-shadow-mirror-spec.md && rg -q "state-coverage revision" .docs/ai/phases/e0a-shadow-mirror-spec.md`.
- [x] **`simmersmith-poj`: release poll propagation-gap regression.** Added a parser seam that treats absent ASC build/state as retryable while preserving VALID success and INVALID/FAILED failure. Verify: `bash scripts/test-release-ios-poll.sh && bash -n scripts/release-ios.sh` (passed).
- [x] **e0a P1b persistence contracts/archive.** Scoped manifest/envelopes/outbox/receipt/canonical contracts; secure full-CKRecord archive; durable asset rebinding; explicit WAL high-water; fail-closed integrity validation. `tier_floor: senior` · `complexity: L`. Verify: `swift test --package-path SimmerSmithCloudKit` (526 passed).
- [x] **e0a P1c transactional writer/recovery.** Added serial writer: fsynced framed WAL/assets, immutable record-first/state-second generations, atomic verified `current`, contiguous high-water replay, exact sent/ack/rebase/supersession, failpoints, and quarantine; no engine/session/app edits. 545 package tests passed. Verify: `swift test --package-path SimmerSmithCloudKit`.
- [x] **e0a P1d engine/session shadow wiring.** One mirror gate orders store/generation/outbox/state coverage; dedicated GCD WAL/generation lanes publish immutable high-water checkpoints without a Swift-task blocking bridge; isolated round-trip validation, safe-state history, exact save/delete failure delivery, sticky quarantine, and fenced clear/park/account changes are pinned. Active boot is nil-state/full-fetch and never hydrates from shadow. Verify: `swift test --package-path SimmerSmithCloudKit` (561 passed) + focused Thread Sanitizer + generic iOS build (passed).
- [x] **`pto` Previous Releases + build 159.** Feature `8c3884e`; GLM implementation, Opus adversarial review, Luna final clearance; local 95 app tests + generic iOS build green. Final-tree CI `cf04409` run 29466604971 green; release `4804c5a`; ASC build 159 `VALID` + explicit Finklea Dev assignment. Bead closed. No cold-start change. Verify: `gh run view 29466604971 --json conclusion,jobs` and ASC beta-group builds report 159/VALID.
- [x] **`simmersmith-1qg` Ballast voice parse + build 160.** P1–P7, independent review, sibling-layout proofs, adapter 36 + Kit 187 + mock 60/60 + generic iOS build green; merged `2d1f506`; ASC 160 `VALID`. Default OFF and not exercised; P8/`zyp` separate.
- [?] **e0a P1e hardening evidence + phase gate — build 159 device rerun pending.** Sel/build 157 captured 35 WAL fsyncs (p50 0.03608 ms, p95 0.22242 ms) then safely quarantined a valid 65-GroceryItem repair loop under forced packet loss. Root cause fixed TDD-first: full survivor reporting is preserved, but adapters persist only changed keepers/tombstones/repoints; mutation test reproduces 57 passes/100 ms on the old path vs two after the fix. CloudKit 564 + Kit 187 + app-target 90 + generic iOS build green. No cache-first/P2 behavior. `tier_floor: senior` · `complexity: M`. Verify: `swift test --package-path SimmerSmithCloudKit && xcodebuild build -project SimmerSmith/SimmerSmith.xcodeproj -scheme SimmerSmith -destination generic/platform=iOS CODE_SIGNING_ALLOWED=NO`.

## Blockers
- `51d`: two-device attendee recency still needs Roshar or another second device. Build 159 is TestFlight-ready while the owner is remote; connected Sel still awaits the candidate install/manual UI. e0a P1e must repeat online/offline recovery and show no new quarantine; shadow-only P1 intentionally does not shorten launch.

## Open questions
- none — owner approved e0a P1 completion + release operations 2026-07-15.
